[Unit]
Description=Bonfire first exact-release persistent ingress guard
After=local-fs.target
Before=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/bonfire-bootstrap-ingress-guard apply

[Install]
WantedBy=multi-user.target
