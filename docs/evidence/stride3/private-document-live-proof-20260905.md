# First real private document and process restart

Status: **verified local nonproduction proof**, 2026-09-05. This is not production activation, a persistent service, a hired agent marketplace, an external-source reader, or an autonomous business outcome claim.

## Exact implementation and useful result

Kernel source `0b6d4585f560cd649136c9d0c98d701217b32901` includes the reviewed business-only adapter, corrected token-count schema projection, explicit HTTP/1 transport, durable provider journal, and operator proof runner. The input is reconstructed from immutable Business mission/customer/first-outcome fields. Independently permissioned sources are rejected until source-aware eligibility and revocation exist through recovery and delivery.

The actual generated brief proposes a narrow founder-operator customer and two consecutive weekly operating reviews as an experiment. It distinguishes supplied context, assumptions, proposed action and falsification. Root inspected the Markdown; no customer contact, evidence of demand, publication, purchase or revenue is claimed.

Private artifact: `/Users/ajhart/Documents/STRIDE/2026-09-05/first-customer-experiment.md`.

## Observed execution

- Existing provider credential was used without printing or persisting its secret. A public-string input count identified its actual provider project before binding the host grant. The model-list endpoint returned200 and listed the intended model. Those connection checks did not generate a document.
- Three local preparation attempts stopped before Work admission while schema/transport problems were diagnosed. Retained failed preparation records are local only; no generation came from them. The count guide's broad same-payload language did not match the endpoint's strict schema. The corrected count projection preserves all nine supported fields. A Go1.26 transport inherited HTTP/2 negotiation while its handler was disabled; explicit HTTP/1 protocols and ALPN fixed the independently reproduced failure.
- The final preparation counted **195 input tokens**, admitted exactly one Work under one generation/$0.10 host cap and matching Business/mandate reservation. Counting is separate provider egress; its cost is not represented as verified free.
- Process **48995** called one Worker step, persisted accepted response identity, returned `reconciliation_required` and exited.
- A new process **49188** loaded the same private state, retrieved the saved response, wrote the terminal receipt and completed Work. It did not create another model response.
- A third process **49527** returned the same terminal result without another provider operation.
- Work `work_53bec66b-2a12-40b0-bc5c-a6fcf8686ff5`; Result `result_a6f8003e-643f-4815-9388-38ae6cde7b54`, eligible under current local Business/Work authority. One provider journal and one result existed after completion and replay.
- Observed model `gpt-5.6-terra`, tier `default`; input195, output1052, reasoning subset78, total1247, cached0, cache-write0. Retained tariff calculation: **13,014 USD micros ($0.013014)**. This is cost calculated from provider usage, not invoice reconciliation.
- Provider reservation ended held0, settled13014, slot_reserved=false. Exact receipt settlement used the immutable provider fact.

## Validation and retention

Combined real PostgreSQL/race transport, count, bridge and adapter suite passed **2.039s**. The adapter's13 integration scenarios cover restart, missing usage, early acceptance/malformed tail, late receipt persistence after cancellation, source/credential rejection and current Business denial. The HTTP/2-capable local TLS test proves HTTP/1 plus separate connections. Independent transport/count/CLI/adapter gates passed within this restricted scope.

Private state/receipts and PostgreSQL custom dump retained under `/Users/ajhart/Documents/STRIDE/2026-09-05/private-proof`, directory0700/files0600. Dump95,416bytes, SHA256 `11ac3c07d0a9be8a6b2bcb27598571a4e390051d147607b612cb47ede35196e7`; archive listing verified. This is not a performed restore drill. The local cluster was stopped cleanly with its data preserved; production was untouched.

## Remaining product gates

Production Business DB/role/config and operator-owned grant provisioning; authenticated AJ Builders membership; first-use UI preparation and result reader on the actual run; service-owned cancellation/reconciliation; source-aware result eligibility before external-source support; an observation-informed next episode; broader harness, meeting intelligence, media qualification and web/native design completion. Main app still serves02cceb; iOS77 predates shared design changes.
