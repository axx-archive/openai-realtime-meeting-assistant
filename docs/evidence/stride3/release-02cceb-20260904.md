# Preparatory release acceptance — 2026-09-04

This is a verified preparatory release, not completion of STRIDE3.0 or evidence of an autonomous business.

- Source: `02cceb6aae96f50b5ab2910d049f5b4afbbeda88`, pushed to `axx/main`.
- Active VPS ledger generation253. Bundle `230dfba983781920a0eb161b2d85d0bd4c5ab65e4c15b26b8901ab5a22ac4368`.
- Application image `sha256:2e289e5c2b73ac936492706cb463aba145338c42403a9e64f6a2d751fa39175b`; renderer `sha256:1c9484fe74a89a3667a37306abd913ed66658be43a818b026457af25757313ff`.
- Retained verifier and subsequent observation both returned `verified-local-unsigned`. Public health/readiness identities agree. All14 rooms were empty before activation. Exact rollback0fae and its images retained.
- Independent browser verification as AJ loaded Home, Work39projects/6active, Bonfire Strategy and hydrated exact-version review controls, Conversations and Rooms. No production review was fabricated and no call was joined for this check.

The preceding candidate6a activated at generation251 and passed infrastructure verification but failed authenticated Home/Work with409 `organization_execution_unavailable`. It was rolled back to0fae at generation252, verified and rendered before correction. W4's signed migration maps its opaque organization ID to the existing Bonfire tenant namespace; comparing these strings directly was incorrect.02 recovers that authenticated mapping without changing membership or retargeting data. In the exact candidate image, a network-disabled/read-only-volume probe validated all62 applicable real sessions and denied0. The probe prints counts only. Focused organization/W4/CLI tests passed6.439s, and independent code review passed.6a's integrated suite had8293passed/20skipped/zero failures; mobile713+TypeScript and web behavior checks passed.

Private operational evidence: `/tmp/stride3-release-{probe,activate,verify,observe}-02.log`. Never publish private session material or provider credentials with this receipt.

## iOS

Exact EAS build77 is `69bf70fe-e4e2-45d8-a650-00fb5aa9bab1`, source6a. Its mobile tree is byte-identical to02. The36,919,502-byte IPA has SHA256 `a2e8acdabb9b7a45cef0f5a4ca31d8f647e5727cf44b1a367f45ff307abec3e5`. All240ZIP entries passed CRC and `codesign --verify --deep --strict` passed. Main app `xyz.thebonfire.app` and broadcast extension `xyz.thebonfire.app.broadcast` are1.0.0(77), iPhone+iPad, arm64, minimumiOS16.4. Submission147c22a9-533b-4f1c-a7ba-0e3869fdb900 completed successfully: EAS confirmed upload to App Store Connect. Apple processing/TestFlight availability remains pending. Physical-device media and recovery remain unqualified.

## Cleanup

One exact QA message was deleted through normal authenticated controls after a verified private before-image. `CHANNEL-PROD-CANARY-0816A` in Bonfire Chat was the only removed message:326→325, all surviving complete message payloads identical, no additions. Its derived source was tombstoned and channel digest invalidated. Riff canary remains pending because the closed historical-pass UI currently hides deletion; a scoped control fix is being implemented without reopening the pass. Four exact AJ-owned zero-message threads were subsequently archived through normal UI controls; two other-owner private empty threads were left unchanged. Independent source verification is pending. Broader exact QA artifact cleanup remains pending.
