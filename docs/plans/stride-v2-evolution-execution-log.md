# STRIDE v2.0 Evolution — Execution Log

Date created: 2026-09-01
Owner: product (AJ Hart) / goal-loop sessions
Primary plan: `docs/plans/stride-v2-evolution-execution-plan.md`
Strategy doc: `docs/plans/stride-v2-evolution-plan.md`
Branch: `main`

---

## How To Use This Log

1. Execute one wave at a time. Do not begin the next until this log marks the current wave `completed`.
2. After each wave, update: wave status, checklist, files changed, migrations applied, validation commands + outcomes, risks/blockers, and the prompt for the next wave.
3. **Compact previous waves** before writing the next-wave prompt (see Log Compaction).
4. Print the next-wave prompt in your final response so the user can continue in-session or paste it into a fresh session.

### Log Compaction

After completing a wave, compact ALL previously completed waves to:

    ## Wave N
    Status: `completed`
    Files: `file1` (created, N LOC), `file2` (modified, +a/-b)
    Risks carried forward: {only unresolved risks, or "none"}

Keep the CURRENT wave's section complete until the next wave starts. Future wave scaffolds hold only wave number, status, and scope checklist.

---

## Agent Team Structure

The executing agent is the lead: it coordinates, delegates implementation, validates, and updates this log. Spawn teammates via the Agent tool, omitting `model`.

| Role | subagent_type | Responsibility |
|---|---|---|
| lead | (executing agent) | Coordinates, delegates, validates, updates log, writes next-wave prompt |
| backend-dev | general-purpose | Go handlers, stores, tests |
| frontend-dev | general-purpose | `index.html` markup/CSS/JS and the `TestIndex*` pins |
| reviewer | feature-dev:code-reviewer | Reviews the wave diff for correctness, patterns, edge cases |
| visual-qa | general-purpose | Rendered pass on the touched surface, both themes, desktop + phone |
| ops-agent | general-purpose | Checkpoint waves only; follows `AGENTS.md`; asks AJ before activation |

Every subagent prompt carries the concurrency clause: never `git checkout/reset/restore/stash/clean`; never overwrite changes you did not make; re-read and re-apply on Edit conflict; do not touch `apple/`.

| Wave | Dev Role(s) | Optional Specialists | Notes |
|---|---|---|---|
| 1 | backend-dev (D2, D7, D8), frontend-dev (D1, D3–D6) | visual-qa | Backend first; frontend consumes D2/D8 |
| 2 | frontend-dev (tokens, glass, menu component), designer (icon inventory, doc) | visual-qa, critic | Design canon is high-stakes; AJ ratifies swatches |
| 3 | frontend-dev, backend-dev (D3, D5, D6) | visual-qa | |
| 4 | frontend-dev ×2 (doc / deck), backend-dev (D2, D3) | visual-qa | Two editors in parallel |
| 5 | backend-dev (ACL, links, versions), frontend-dev | visual-qa, critic | ACL is high-stakes |
| 6 | frontend-dev, backend-dev (WS verbs) | visual-qa | Media pins are brittle |
| 7 | backend-dev, frontend-dev | visual-qa | Recording consent copy |
| 8 | backend-dev ×2, frontend-dev | critic | Memory design is high-stakes |
| 9 | backend-dev | reviewer | Founder-gated |
| 10 | frontend-dev | visual-qa | |

---

## Ops Debt Tracker

**Deploy order (canonical, see `AGENTS.md` and `deploy/digitalocean/README.md`):**
1. `go test ./...` green locally (no Go on the VPS).
2. Commit + push to `axx/main` (fetch and rebase first, never force-push).
3. Prepare a clean worktree at the exact commit; scope/prepare the release archive.
4. Upload in 2MB chunks; build on the droplet from the archive.
5. Confirm `/readyz` participants == 0; activate from the serving release's sealed tool.
6. Verify `/healthz` + `/readyz` release identity, then rendered smoke on thebonfire.xyz.

### Pending Ops

| Source Wave | Type | Item | Status |
|---|---|---|---|
| 10 | env (optional) | `GIPHY_API_KEY` — GIF search 503s honestly without it; import works keyless | pending AJ (not required for activation) |
| 8 | env default | `BRAIN_DECAY_DAYS` — decay above transcripts; server default applies when unset | no action (default) |
| 5 | scheduled job | weekly blob sweep now wired from the trash sweeper: first pass is a dry-run report, deletion only after the two-sighting window (7 d); reference walker covers deck scene refs + version SceneRef, fail-safe refuses on zero refs | verify the first dry-run log line after activation |
| 7 | consent lane | recording lane default OFF; guests need an explicit `recording` grant; media stored in blobs | no action (default off) |
| 5 | boot data migration | one-rewrite Drive ACL stamp via `updateEntriesWithMetadata` on first boot (idempotent) | snapshot the data volume before activation (ceremony does) |
| 8 | boot job | channel-ingestion drift backfill now runs as a goroutine after boot (bounded 2000 rows; idempotent) | watch the backfill log line |
| 9 | verifier | `/readyz` lane semantics changed: `idle` no longer degraded; `paused_by_breaker` counts as degraded; `fallback_active` distinct | release verifier expectations updated for the activation |
| 2 | identity | `/auth/me` gains `organization` (workspace name; `STRIDE_ORGANIZATION_NAME` optional) | no action |
| 12 | secret (future) | `ANTHROPIC_API_KEY` staged at `/root/secrets/anthropic.key` (root, 600) — applied as a base-env patch at OPS-8, never before | hold |
| — | schema | no SQL migration in this batch (`BONFIRE_CANONICAL_MIGRATION_MAX_VERSION=25` unchanged) | none |

### Ops Checkpoints

| Checkpoint | After Wave | Trigger | Covers |
|---|---|---|---|
| OPS-1 | 1 | Conversations complete | Wave 1 |
| OPS-2 | 4 | Design system + Work + Studios complete | Waves 2–4 — folded into the OPS-6 batch activation (2026-09-02) |
| OPS-3 | 5 | Drive complete | Wave 5 — folded into OPS-6 |
| OPS-4 | 7 | Rooms complete | Waves 6–7 — folded into OPS-6 |
| OPS-5 | 8 | Memory complete | Wave 8 — folded into OPS-6 |
| OPS-6 | 10 | Program complete | Waves 2–10 as ONE exact-release activation with rooms empty (AJ authorized 2026-09-02); commit scoping excludes the other session's `apple/` tree, `docs/plans/stride-native-macos-*`, `docs/qa/STRIDE-Native-Media-Local-QA.txt`, `docs/qa/stride-macos-media-ab.md`, `scripts/build-macos-dmg.sh` |

**Hotfix (2026-09-02):** `hotfix/scout-source-lock` → `c1fdd9da` (pushed to axx): `lockCurrentCompanyConversationSources` now drops transcript rows whose message was deleted or whose `EditedAt` predates the lock start, keeps failing closed on unexplained drift, metadata mismatch (incl. channel rename), archived threads and the ledger branch; 11 subtests from the production shape; deploy in flight. Wave 8 D12 root cause landed in the shared tree (edit re-files under a revision id, delete tombstones). Known-by-design residual: an edit landing between context assembly and the lock is dropped rather than refused for transcript-only contexts. Test hygiene: `TestRoomsHandlerCreateListAndActions` / `TestRoomsHandlerArchiveRestore` depend on a package-global set by earlier tests (fail alone with `meeting record store unavailable`) — filed as a task.

**Session note (2026-09-02 ~06:50 ET):** a second usage-limit interruption terminated the four in-flight frontends (Drive, memory, around-call, status pill); index.html still parsed and Go built; all four resumed from their transcripts.

**Session note (2026-09-02 ~01:50 ET):** a usage-limit interruption terminated six agents mid-task (hotfix branch, Wave 3 frontend, Wave 7 backend, Drive A follow-ups, memory re-file, Drive review); the tree still built and vetted clean; all were resumed from their transcripts.

### Applied Ops

| Date (UTC) | Checkpoint | Item | Evidence |
|---|---|---|---|
| 2026-09-02 01:22 | OPS-1 | Wave 1 `e448f2db` live on thebonfire.xyz, ledger generation 243 → 244; diagnostic rollback to `97672478` (gen 245, 01:32) proved the Scout "company source changed" refusal pre-exists; re-activated `e448f2db` (gen 246, 01:36, verifier ok, `chatGroupsLabel` served, embeddings healthy) |
| 2026-09-02 06:17 | hotfix | `c1fdd9da` (tolerant company-source lock) live, gen 246 → 247, verifier ok, receipt `4c1fec78…`, image `37cf1973…`; private-thread probe at 06:19 answered in 2 s with no refusal (Scout typed lane alive again). Gate note: `TestDenseChatTailRequestAndEarlierMergeRendered` flaked in the release worktree and 3/10 on pristine `e448f2db` — pre-existing rendered scroll-visibility flake, not the hotfix | receipt sha256 `fa7c632b…`, bundle `6e1ac9d1…`, app image `d25d10ab…`, render image `c7fb3f1d…`, verifier `verified-local-unsigned`; no migration (0001–0025 unchanged, `BONFIRE_CANONICAL_MIGRATION_MAX_VERSION=25`); rooms empty at activation; `/conversations` serves `chatGroupsLabel`; `/assistant/chat-search` 401 unauthenticated; prior release `97672478` retained for rollback; activation log `/root/ops-activate-e448f2db.log` |
| 2026-09-02 | batch commit | Waves 2–10 committed on main as `4f15435e` (154 files, +42,216/−3,180; `apple/` and the other session's four files excluded); pushed to axx/main; OPS-6 activation follows | critic round 2 8.7 with the last item settled (force-accept protocol, scores attached); full suite `full-batch-2.txt`; e2e section |
| 2026-09-02 16:25 | OPS-6 | `722f5cf6` (Waves 2–10 batch `4f15435e` + docs stamp) live on thebonfire.xyz, ledger generation 247 → 248 (idle gate passed 16:23:45Z, 14 rooms / 0 participants, realtime not connected; activated 16:25:45Z from the serving `c1fdd9da` sealed tool, ordinary activation, `baseEnvPatchMode` null, `qualificationState` null, Anthropic key untouched); verifier `verified-local-unsigned`; release receipt sha256 `1e926cf8…`, bundle `87f69211…`, app image `247a5ca4…`, render image `1a8e2f79…`, binary `54753078…`, source archive `a29e632a…` (15×2 MB chunks, sha256 matched both ends), tree digest `ddf1c3c2…`; no migration (0001–0025 unchanged); pre-activation state snapshot `/opt/meetingassist-backups/pre-activation-722f5cf6-20260902T162210Z.tgz` (298 entries, 102 MB; `blobs/` + `backups/` excluded); prior release `c1fdd9da` retained as `previous` for rollback; activation log `/root/ops-activate-722f5cf6.log`, build log `/root/ops-build-722f5cf6.log` | `/healthz` + `/readyz` release identity = `722f5cf6…`, `/readyz` `ok:true`; lanes: scout / typedScoutAnswer / typedScoutRouter / stt / meetingSTT / privateVoice / roomVoice / dictation / workflows = `idle` (no longer `degraded`), embeddings / brain / recap = `healthy` (circuit closed); `degraded[]` = six ambient producers (no run yet since boot), `backup` (first snapshot boot+3 min, offsite dormant), `canonical_runtime_degraded` (shadow mode, pre-existing); re-checked 3 min after boot: unchanged, 0 participants. Boot lines: `Drive visibility migration stamped 1 unlabeled file row(s) company` (16:24:35Z); `channel ingestion backfill: superseded 4, tombstoned 2, skipped 17 (examined 194 live rows)` (16:25:17Z); `Weekly blob sweep dry run: scanned=5760 unreferenced=89 pendingFromLastRun=0` (16:35:17Z, boot+10 min via `startFileTrashSweeper`; reported only, deleted nothing — dry-run-first by construction, `blobs.go:721-729`). Errors: one-shot boot burst of 102 identical `SourceEpisode Work artifact restart reconciliation unavailable: source episode authority denied` at 16:24:30Z (message exists in the `c1fdd9da` tree; not repeated in the following 20 min), plus OpenAI-side `max_output_truncation` on day-digest/mission-intel and pre-existing late-source finalization messages; no panics, no error storm. `/auth/me` unauthenticated → 401. Rendered Playwright smoke run by the lead session; authenticated smoke skipped (no keyless-safe login path on prod) |

---

## Wave 1

Status: `completed` (built, reviewed twice, critic ACCEPT 9.0, /code-review high findings fixed and re-reviewed, committed on main)

### Scope Checklist
- [x] D1 Human-group sidebar section + create form + member picker + third render bucket (`groups · you + people`, `#chatGroupCreate`, avatar cluster rows, `N people · shared memory` eyebrow)
- [x] D2 `PATCH /assistant/chat-threads/{id}/members` (owner-only; add ∪ remove; human group <2 → 409; unregistered → non-enumerating 400; project threads min 1; lock → re-read → save → continuity rebuild → `chat_thread` fan-out to prior ∪ new members)
- [x] D3 Member management popover (glass-float, owner sees `×` + add row, optimistic with rollback, viewer removal drops the row and falls back to a channel)
- [x] D4 Server read markers (`POST /assistant/threads/read` on select / tail-in-view / append, 400 ms debounce; unread dot and seam from `unreadCount` / `lastReadMessageId`; phone seam unlocked)
- [x] D5 Typing indicators (`chat_typing` frames ≤1/3 s, false on send/clear/blur/switch; presence row with 4 s TTL; never self or Scout; reduced-motion safe)
- [x] D6 Mute / notification level (bell popover: All / Mentions only / Nothing → `/assistant/threads/mute`; index rows carry `muted` + `notificationLevel` only when non-default; bell-slash glyph; dot suppressed at `none`)
- [x] D7 Mention roster from accounts (`chatMentionCandidates` enumerates `accountStore().accountEmails()`, Scout first, viewer excluded, alphabetical; composer autocomplete uses the server directory once loaded — critic round-1 fix)
- [x] D8 `GET /assistant/chat-search` (viewer-projected, non-archived threads, 2..200 runes, limit ≤50, newest first, ±60-rune snippets) + sidebar results with `<mark>` ember-10% wells, ↑/↓/Enter/Escape, jump-to-message with ≤5 pages of history paging

### Files Changed
- `index.html` (+~2,250/−~20): CSS 10318–10805, 11235–11510, 16389–16445, 17308–17330; markup 36016–36161; JS 38041–38062, 38462–38477, 39935–40080, 62219, 65762–65909, 66117–66344, 67097–67160, 68780, 71072–72318 (the Wave 1 block), 72392–72650, 73427–74433.
- `scout_chat_threads.go` (+324/−63, of which ~25 hunks are the other session's human-group backend, folded in deliberately): members PATCH branch, `updateScoutChatThreadMembers`, creation fan-out, group preview copy.
- `chat_search.go` (new, ~230 LOC), `chat_participants.go` (+42/−?), `thread_mute.go` (+9), `thread_read_markers.go` (+18), `main.go` (+1), `authorization_surfaces.go` (+2), `guest_allowlist_test.go` (+1).
- Tests: `chat_search_test.go` (new), `scout_chat_members_test.go` (new), `frontend_conversations_wave1_test.go` (new), `chat_participants_test.go` (+81), `thread_read_markers_test.go` (+90); the other session's `scout_chat_human_group_test.go` (untracked → committed with the backend it pins).
- Docs: the three `stride-v2-evolution-*` documents (this program's plan, execution plan, and log).

### Migrations
None.

### Validation
- `go build ./... && go vet .` → clean.
- Focused Go: `go test -count=1 -run 'TestScoutChatMembers|TestChatSearch|TestChatMention|TestAssistantHumanGroup|TestScoutChatThreadsIndex|ThreadReadMarker|ThreadMute|TestAuthorizationSurface|TestGuestRouteWalkAllowlistFailsClosed' .` → 35/35 PASS before the critic fixes; re-run after them recorded below.
- Broad Go slice `-run 'Chat|Mention|Thread|Participant|Riff|Notification|Authorization|Guest'` → ok (164.8 s).
- Frontend battery `-run 'TestIndex|TestFrontend|TestChat'` on a detached worktree at HEAD (with `node_modules` linked) → 0 failures baseline; on the Wave 1 tree → 0 failures, 0 new.
- Full `go test -count=1 -timeout 40m .` → three runs on the Wave 1 tree: run 1 (before the critic fixes) `ok` 837.9 s; run 2 (final tree, no contention) one failure, the pre-existing room-archive flake documented below; run 3 (post-review tree) one failure, `TestRenderedChatMutationsStayBoundToSourceAndAuthGeneration` (1.14 s, pixel-tolerance rendered test) which then passed 10/10 in isolation on both the Wave 1 tree and pristine HEAD and 3/3 inside the rendered-chat battery under mutual contention — classified as a load flake (three finder agents were grepping the tree during that run). Every other test was green in all three runs. Note: `go test .` needs `-timeout 40m`; the default 10 m kills the suite.
- Rendered/end-to-end (sandbox :3171 — binary provenance: built from the tree before the critic round-1 backend fixes for the lead's pass, rebuilt from the final tree afterwards; the critic's round-2 pass used its own build of the final tree on :3172 — AJ in the browser pane, Tim via API and Playwright, Tom via curl): group create 201 + idempotent replay 200; members PATCH 200 / 403 non-owner / 404 outsider / 409 shrink-to-one with rollback and server copy in the toast; removed member's row drops in ~150 ms; read marker POST fires on open and the server unread count goes 1 → 0 with the marker on the newest message; mute `none` suppresses the dot; typing frames start/stop on the socket, "Tim is typing…" rendered cross-user with 4 s expiry; search results mode hides section heads, `<mark>` computes to ember at 10 %, click jumps to and rings the message in view; XSS probe (`<img onerror>`) rendered as literal text in snippets and messages; another user's private thread never returned; both themes at 1280×800 and 390×844.
- Code review (feature-dev:code-reviewer): backend pass no findings; frontend pass one finding (singleton add-picker kept chips across thread switches) → fixed with an `onClose` reset → re-check PASS.
- `/code-review high` (recall-biased: 7 finder angles × ≤6 candidates → 4 batched verifiers): 10 findings survived — non-seed @-mentions never notified (`chat_mentions.go:156`), index fingerprint `id:updatedAt` stranded mute/read state from other devices (`index.html:65967`), search results mode hid conversation-name matches, members render closed the bell menu on channels, typing/search names empty for non-seed accounts (`chat_typing.go:28`, `chat_search.go:118`), UTF-16 query gate + 400 shown as outage, search projected every thread per request, membership change skipped `reconcileConversationSourceEpisodeAuthority`, single pending read-marker slot. Refuted: the ACL-leak reading of the reconcile gap (reads fail closed), mobile unread on open (both open paths force the tail), unknown `conversationKind` records (no writer can produce one), and "delete the seen map" (it feeds the `since=` tail hydration). All ten fixed in a third dev round: backend `chatMentionTargetEmails` (directory-resolved mentions), shared `accountDisplayName`, `reconcileConversationSourceEpisodeAuthority` after membership change, fan-out via `deliverScoutChatThreadMetadata` + `{id, removed:true, title}` to ejected members, 32-member cap on project threads, search rewritten with a raw-record prefilter, riff exclusion, parsed-once times, bounded newest-first insertion and an ASCII fast path, one-pass viewer mute map, `clampQueryLimit`; frontend fingerprint with viewer-state fields, `muted` collapsed into `notificationLevel`, "conversations" group above "messages" in search, members-hide closes only its own popover, code-point gate + 200-cp ceiling + 400→validation copy, cross-thread read-marker flush, length-preserving fold for `<mark>`, `chatNotifyPending` merge guard, `removed:true` handling, directory state hoisted (TDZ guards deleted), shared icon/avatar nodes. Reviewer re-checks: backend PASS (8/8), frontend PASS (hoist has no duplicate declarations, read-marker optimistic state is not resurrected, fold keeps offsets aligned, search traversal wraps across both groups, avatar special cases survived). The critic was not re-run after this review round: its 9.0 acceptance predates the fixes, which only remove defects it did not observe; Wave 2 runs the recall review BEFORE the critic. New file `chat_directory_test.go`.
- Pre-existing flake, not Wave 1: `TestArchivedRoomRejectsMemberAndGuestWhileDurableCloseRetriesBeforeTeardown` fails 3/20 on pristine HEAD (`git worktree` at `97672478`) and 3/20 on the Wave 1 tree, always at ~1.02 s; filed as a separate task. It appeared in two of three full-suite runs; each other test was green.
- Critic (goal-anchored, threshold 9.0 / floor 7.0): round 1 REJECT 7.8 → seven revisions applied (creation fan-out, "new group" preview, archived threads excluded from search, composer roster from the server directory, rAF → setTimeout for popover focus, "search unavailable" state, log + gap map + founder decision 6) → round 2: ACCEPT 9.0 (goal fidelity 9 / product bar 9 / design 9 / truthfulness 9 / plan durability 9); the critic re-verified the backend fixes on a binary it built from the final tree (:3172) because the lead's :3171 sandbox binary was stale after the backend edits (restarting only re-reads index.html) — rebuilt afterwards.

### Risks carried forward
- Search runs the full viewer projection over every readable thread per query (linear, no index); revisit in Wave 10's snappiness budget if channels grow past a few thousand messages.
- Mention-candidate order changed from roster order to alphabetical; the macOS client should be checked for any ordering assumption.
- Non-owners cannot leave a group (founder decision 6).
- The account store has no lifecycle concept (no disabled/deactivated flag), so an offboarded person stays a valid @-mention target and group member until their record is deleted (plan §Gap map, Wave 5).
- Cross-user typing rendering was verified in the frontend dev's two-user run and by server tests; the lead verified the client frames only.
- Design nits for Wave 2 to absorb (critic): a non-owner's members dialog has no focusable child so focus stays on the trigger; group rows lead with initials discs while other rows lead with a stroked glyph — ratify or unify in the icon system.
- Harness lesson: restarting the sandbox re-reads `index.html` but not the Go binary — rebuild `ma` after any Go edit before making rendered claims.

### Ops Checkpoint (OPS-1)
Status: `completed` 2026-09-02 01:22 UTC — see Applied Ops.

### Prompt For Wave 2

```
Continue the STRIDE v2.0 Evolution implementation on branch main (shared working tree; never git checkout/reset/restore/stash/clean; do not touch apple/).

Source of truth:
1) docs/plans/stride-v2-evolution-execution-plan.md — read ONLY the Critical Rules section and Wave 2 details
2) docs/plans/stride-v2-evolution-execution-log.md — read ONLY the Wave 2 section and the Ops Debt Tracker (Wave 1 is compacted)
3) docs/stride-signal-canon.md §mark and §color only if the inline context below is not enough

Execute ONLY Wave 2 (Design system canon — color, glass, icons, menus). Do not start Wave 3.

## What previous waves shipped
Wave 1 (the main commit titled `feat(chat): Wave 1 — groups, membership, read markers, typing, mute, search`; find it with `git log --grep='Wave 1' --oneline`): human groups, membership route + popover, server read markers, typing, mute, roster from accounts, conversation search — all in index.html + Go. Its two header popovers (`#chatConvoMembersPopover`, `#chatConvoNotifyMenu`) and `chatHeaderPopoverController` (index.html ~71690–71715) are the freshest consumers of the ad-hoc glass recipe and must migrate to the shared menu component in this wave.

## Wave 2 scope
- D1 Palette canon as tokens + `docs/design/stride-design-system-v2.md` with rendered swatches for both themes; ember doctrine and its exceptions written next to the token.
- D2 Liquid-glass tiers `glass-chrome` / `glass-float` / `glass-sheet` as tokens + classes with no-backdrop-filter fallbacks; migrate every translucent surface (inventory first).
- D3 One icon system based on `strideChatActionIcon` (1.8 stroke, currentColor, 16/20/24); inventory the 203 inline `<svg>`s and replace stragglers.
- D4 One menu component `bfMenu(trigger, items, {origin, radio})` (glass-float, @starting-style from trigger origin, --ease-spring, role=menu/menuitem/menuitemradio, arrows/Home/End/type-ahead, Escape + outside close with focus return); migrate: reaction picker, chat more-menu, greenroom filters, account menu, files kebab, org menu, settings sections, Wave 1 members/bell popovers, deck + document editor menus (18 `role="menu"` sites today).
- D5 Theme-parity pass with the canvas-resolved contrast probe → 0 failures pinned by a test that walks token pairs.
- D6 Toasts, focus rings, selection, scrollbars, empty-state art unified on the tokens.

## Inline Context
- Light tokens start at index.html:76 (`:root {`), dark overrides at :406 (`:root[data-theme="dark"]`-style block); `--surface-1` :150 / :406, `--text-3` :175 / :415, `--ring` :194 / :425, `--ember` :208 (`var(--ember-500)`), `--ember-text` :226, `--glass-blur-chrome: saturate(1.8) blur(20px)` :283, motion tokens :367–368 (`--ease-spring`, `--dur-fast`).
- The current glass recipe (33 sites reference `--glass-blur-chrome`): `background: color-mix(in oklab, var(--surface-1) 94%, transparent); backdrop-filter: var(--glass-blur-chrome) saturate(1.25); transition-property: opacity, transform, display; transition-duration: var(--dur-fast); transition-timing-function: var(--ease-spring); transition-behavior: allow-discrete; @starting-style { opacity: 0; transform: scale(.96) }` with `transform-origin` set from the trigger. 13 `@starting-style` blocks exist.
- `strideChatActionIcon(name)` at index.html:75242 returns inline SVG strings for react/riff/reply/more at 1.8 stroke — extend, don't fork.
- Rules that bite: deck editor first frame must have zero animations (`TestDeckStudioRenderedPhoneTouchControls`); fullscreen overlays enter opacity-only; `reducedMotion` is declared at ~38374 — gate all motion on it; rail mark stays graphite (#54545C light / #77777D dark), never `--text-1`; the contrast probe must resolve `color(srgb …)` through a canvas round-trip.

## Execution (Team)
You are the lead. Spawn via the Agent tool, omitting `model`. Task tools may be unavailable — keep the checklist in the log.
1) Spawn `frontend-dev` (general-purpose): D1 tokens + D2 glass tiers + D4 menu component + migrations, with the inline context above and the concurrency clause.
2) Spawn `designer` (general-purpose): D3 icon inventory + replacements, and the design-system doc with swatches; coordinates names with frontend-dev via SendMessage.
3) When both return: spawn `reviewer` (feature-dev:code-reviewer) on the diff; relay fixes; re-check.
4) Spawn `visual-qa` (general-purpose): both themes, 1280×800 and 390×844, contrast probe, keyboard paths on every migrated menu.
5) Run the critic-loop skill (threshold 9.0, floor 7.0) anchored to the founder's design bar: palettes, liquid glass, icons, menus.
6) Compact Wave 1 in the log; fill Wave 2; write the Wave 3 prompt.

Critical rules: see execution plan §Critical Rules (concurrency, three-registry, index.html boot read + TDZ, design canon, gates, deploy discipline, memory contract, default-off domains, provider doctrine, honest states, design pass per wave).

Requirements:
1) Keep working on main; commit only after the full `go test -count=1 -timeout 40m .` is green and the reviewer + critic pass.
2) No migrations expected; if any, `YYYYMMDDHHMMSS_` prefix.
3) Record validation commands + outcomes in the log.
4) Append deploy-affecting changes to the Ops Debt Tracker (OPS-2 covers Waves 2–4).
5) Print the Wave 3 prompt in full in your final response.
```

## Wave 2
Status: `completed` — batch gate 2026-09-02 (recall reviews + fixes + full suite + e2e, see "Batch gate" and "End-to-end verify" below); prior note: (both halves done 2026-09-02; reviewer one finding fixed — `--surface-0` textarea → `--well`; critic + recall review run with the OPS-2 batch)
Scope: palette canon as tokens (light + dark), liquid-glass surface tiers, one icon system, one menu component with every menu migrated, theme-parity contrast pins, toasts/focus/empty states unified; `docs/design/stride-design-system-v2.md`.
Designer done: `STRIDE_ICON_PATHS` + `strideIcon/strideIconMarkup/strideIconButton` (index.html ~75609–75750, `strideChatActionIcon` kept as alias), 147 tags normalized to the 1.8-stroke family, 8 fills + 27 text glyphs replaced, 216-site inventory (192 conform; named exceptions: Scout flame fills, CSS form glyphs, brand marks, media placeholder, ring, code); `frontend_icon_system_test.go` (5 pins); `docs/design/stride-design-system-v2.md` (554 lines: palette + swatches, ember doctrine, glass tiers + migrated surfaces, icon system, `bfMenu` contract, motion, new-surface checklist). Frontend done: role tokens (`--ground`, `--surface-0`, `--well`, `--ink-1..4`, `--line`, `--line-strong`, `--shadow-float`, `--selection`), glass tiers `.glass-chrome|float|sheet` (+ `@supports not` fallback ≥98%) with 37 surfaces migrated, `bfMenu()`/`bfMenuAdoptOpen()` (~L62760–63000) adopted by 14 menus with ids intact, D6 base rules; `frontend_design_system_v2_test.go` (6 pins incl. a 144-pair contrast walk at 0 failures both themes). Judgement call for AJ: the family's `more` dots read lighter than the old filled kebabs; the `ember` deck theme (full-orange ground) needs ratification.
Production note (2026-09-02): OPS-1 activation → rollback → re-activation cycle used to prove the Scout "company source changed" refusal is pre-existing (Wave 8 D12).

## Wave 3
Status: `completed` — batch gate 2026-09-02 (recall reviews + fixes + full suite + e2e, see "Batch gate" and "End-to-end verify" below); prior note: (backend + frontend done and reviewed 2026-09-02 — one finding, a duplicate badge-count GET under a kind filter, fixed; commits with the OPS-2 batch)
Scope: live Work list, nav badge, Save-to-Files with fallback, admit all result kinds, ask-for-changes, room work in Work, step progress, version list, honest states.
Backend done: `studio_projects.go` (+388) kinds `image|sheet|research|artifact`, `origin {kind:"room", roomId, roomTitle?}`, `steps[] {id,label,state,at?}`, `result.openAction|primaryAsset|downloadUrl|versions[]` (journal-backed, cap 20); `files.go` (+45) `/assistant/files/save` accepts every studio kind → `{ok, fileId, file}`; tests +391 lines, focused + broad slices ok. Backend review: no findings. Frontend done (index.html +1873/−696 incl. Wave 2 hunks): os_event-driven Work list with a 30 s fallback poll and `#workRailBadge`, kind chips + `?kind=`, `openAction`-driven primary buttons, Save to Files (plain route first; disposition route only on 404/501), Ask for changes (arms follow-up, never launches), versions rail with read-only reopen, room origin, step rail with a reduced-motion-gated pulse, honest per-status copy; `frontend_work_hub_wave3_test.go` (7 pins). Notes: goal-owned image/sheet results need attachments.go binding (deferred); chat renders report `result.type: image` though stored as markdown.

## Wave 4
Status: `completed` — batch gate 2026-09-02 (recall reviews + fixes + full suite + e2e, see "Batch gate" and "End-to-end verify" below); prior note: (backend + frontend done and reviewed 2026-09-02 — one CRITICAL finding fixed: the serializer dropped a trailing root-level text run on save (missing `flushInlineRun()`), rendered regression test in flight; AI assist D9 deferred — no backend route yet)
Scope: autosave, version history/restore, DOCX, 409→copy, print, open .md from Drive, deck Theme + layouts, dead editor removal (+ OPS-2).
Backend done: `studio_versions.go` (`GET /artifacts/{document,deck}/versions?id=`, `?version=N` reopen read-only, PATCH `restoredFrom`), `document_docx.go` (`POST /artifacts/export-docx`), `document_import.go` (`POST /artifacts/document/import`), `deck_theme.go` (Theme graphite/putty/ember + 4 layouts in deck GET), `memory.go` version journal fields; four routes registered thrice. Backend review: no findings (journal growth pre-existing, backlog item). Frontend done: autosave (1.5 s debounce after the first filed save, mono chip, backoff + online retry), 409 → "Save a copy…" / "Reload latest" without discarding the body, History `bfMenu` with read-only opacity-only version stage and restore, Word beside PDF on `expectedVersion`, Print (+ ⌘P), `.md/.txt` Drive import into Document Studio, three theme swatches + Layouts menu, `openDeckEditor` (744 lines) removed, root-level text serializer fix; `frontend_studios_wave4_test.go` (8 pins); 407 pins green. Notes: ember deck theme needs designer ratification; versions list excludes the current revision by design.

## Wave 5
Status: `completed` — batch gate 2026-09-02 (recall reviews + fixes + full suite + e2e, see "Batch gate" and "End-to-end verify" below); prior note: (backends A+B done and reviewed — findings fixed: version chain bridges trashed nodes, passkey login of a disabled account → 401, revoke standardized on 404, `POST /assistant/files/trash/empty`; frontend done and reviewed 2026-09-02 — one finding fixed: the "in content" search tag now mirrors the server's uploader-email match)
Frontend done: Access section (Only me / Company / People + member picker, uploader-only editing), file share links (mint/list/revoke), previewer sheet (img/pdf/video, ←/→/Esc, opacity-only enter), versions rows + "N versions" chip (superseded hidden), Starred + Trash scopes with Restore and a glass Empty-trash confirm, server search (250 ms, ≥2 chars, "in content" tag), `pickDriveFilesForWork()` + "Use in a request", folder roll-up, usage bar, 413 copy, "All files" head, Recent = last opened; `frontend_drive_wave5_test.go` (9 pins incl. no-ember/no-green guard).
Scope: per-file ACL + share with people, previewer, file share links, versioning, starred + trash, content search, attach to work requests, quota, view fixes, blob GC, account lifecycle (+ OPS-3).
Backend A done: per-file `visibility|grants` + `authorizeFileEntry` on every read seam (list/search/versions/trash/share/attachments/Scout context/blob/work refs/import), `PATCH /assistant/files` (access, starred, rename), soft delete + `POST /assistant/files/restore` + 30-day sweep, `?versionsOf=`, `?q=` content search, `GET /assistant/files/usage` + 413 quota, `contextRefs` on chat sends → work launches (403 with nothing launched), one-rewrite boot migration via `updateEntriesWithMetadata`.
Backend B done: `share_links.go` file links (`POST /artifacts/share {fileId}` → `objectType:"file"`, `/a/<token>` streams the blob), `blobs.go` referenced-ref index + weekly two-sighting sweep + `POST /assistant/admin/blobs/sweep`, `accounts.go` `DisabledAt` + `accountIsDisabled` + `PATCH/GET /assistant/admin/accounts` (founder-owner only; disabled accounts refused at session mint and `userFromRequest`, filtered from mention candidates and human-group validation). Reviewer doubts: stranger revoke 404 vs 403; company files mintable only by uploader; passkey login of a disabled account surfaces as 500; two admin notions (approval admin vs founder owner).

## Wave 6
Status: `completed` — batch gate 2026-09-02 (recall reviews + fixes + full suite + e2e, see "Batch gate" and "End-to-end verify" below); prior note: (backend done + reviewed 2026-09-02 incl. the D4 SFU seam — guest bucket + ICE-restart and ungraceful-rejoin tests added; frontend done + reviewed; AJ-driven fixes landed 2026-09-02: room-chat composer ring clipped by the scroll pane (`.room-chat__form` 4 px lanes) and the reaction pill overflowing/off-centre (content-sized `inline-flex`, 44×44 grid cells, centred `--well` disc); two-participant QA in AJ's Chrome against the sandbox: guest joined via minted link, both tiles labelled, hand strip ordered `1 you · 2 guest`, host menu on the peer tile = Mute microphone (they can unmute) / Remove from room, lock toast honest, quality badge hidden while good)
Scope: live captions, reactions + hand raise, device pickers, share as second transceiver + audio, host controls, quality badge, Scout invite decoupled from voice gate, room-chat parity.
Frontend done: captions overlay (last two lines, mono speaker, fade gated on reduced motion), reaction `bfMenu` + tile bursts, hand strip, camera/speaker pickers with `setSinkId` remembered in `bonfire.devices.v1`, share on the second sender pair (mids 2/3 `sendonly`, camera untouched, honest fallback on older servers), host menu per tile + lock, `room_moderate_request` compliance with "The host muted you", removal notice, quality badges, text-mode Scout copy, room-chat link previews + own/peer surfaces, dock on `.glass-chrome`; `frontend_rooms_wave6_test.go` (9 pins); two-participant headless harness 25/25. Flag for review: on phones the Route disc folds away so React's row raises a hand.
D4 seam (main.go): `addRoomPublisherUplinkTransceiversWithSources` offers camera video / mic audio / screen video / screen audio per publisher (H.264 both video sections), `participant_track.source: camera|screen`, paused shares withdrawn from subscribers without renegotiation, share audio forwarded to humans but excluded from mixer/transcription; `sfu_screen_share_test.go` (5 tests).
Backend done: WS verbs `room_reaction {emoji}` (8-emoji allowlist, 1/s), `room_hand {raised}`, `room_moderate {action: mute|remove|lock|unlock, participantId?}` (managers only; `room_moderate_request` then audio-only drop after 3 s; `room_participant_removed`; lock → admission denied / guest join 423; auto-clears when empty); roster fields `handRaisedAt`, `raisedHands`, `hostMutedAt`, `locked`; D7 text-mode Scout (`POST /api/rooms/agents/scout {action:"invite", mode:"text"}`, no provider session) with `scoutText` on responses and `/readyz`. Reviewer: one important finding — the mute grace timer keyed on participant NAME could enforce on a session that rejoined within 3 s and was never asked (fix in flight: capture the exact session/lease); everything else sound (guest containment, audio-only drop, lock/admission under one lock, unique guest names, text-mode Scout starts no provider session). Doubts: member lock enforced at admission not HTTP 423 (office socket shares the path); server mute requires the client to republish audio to un-mute.

## Wave 7
Status: `completed` — batch gate 2026-09-02 (recall reviews + fixes + full suite + e2e, see "Batch gate" and "End-to-end verify" below); prior note: (backend done and reviewed 2026-09-02 — abandoned chunked uploads `.part` leak fixed with a TTL reaper; frontend done and reviewed 2026-09-02 — one critical fixed: the recorder flushed only at 5 MB so the first server consent check could lag minutes; now a 20 s time-based flush + immediate first flush, `partIndex` assigned at upload time, 409 resync from `partCount` when present, 403 stops at once and discards)
Frontend done: lobby rail schedule (`datetime-local` ↔ UTC, attendee picker, 409/403 copy), `/?room=<id>` preselect only (never auto-join), `/?record=<id>` deep link deferred with `setTimeout(0)`, recorder (MediaRecorder feature-detected, stopped first thing in `leaveRoom()`), recap card gated on `meeting-recap-card-` ids with Open record / Open in conversation buttons; `frontend_rooms_wave7_test.go`.
Scope: scheduled meetings + timed ICS + upcoming list, recording to blob → Meeting Record (default off), post-call recap card (+ OPS-4).
Backend done: `scheduled_meetings.go` (`GET/POST /assistant/meetings/scheduled`, `PATCH/DELETE …/{id}`, `?upcoming=1`, `GET /calendar/meetings.ics[?id=]` timed VEVENT with join URL `/?room=<id>` + ATTENDEE lines), `meeting_recordings.go` (`GET/PATCH /assistant/meetings/recording/settings` default off, `POST /assistant/meetings/recording/upload` chunked with `recording` consent lane, 100 MB cap via `putBlobWithCap`, `recording` output stage + `playbackPath`, blob auth), `meeting_recap_card.go` (one idempotent Scout recap message per meeting in the room-named channel, office → Table). Doubts: Meeting Record link uses `?record=<id>` (frontend must honor); recording-only meetings stay invisible until a transcript source exists (pre-existing rule); guests need an explicit `recording` grant.

## Wave 8
Status: `completed` — batch gate 2026-09-02 (recall reviews + fixes + full suite + e2e, see "Batch gate" and "End-to-end verify" below); prior note: (backend D1–D12 done and reviewed; frontend done and reviewed 2026-09-02 — one finding fixed: Drive `contextRefs` now cleared on thread switch)
Frontend done: memory tool rebuilt as "What Scout knows" inspector (filters: subject / person `bfMenu` / since / kind chips; items with glyph, mono time, provenance chips, status; drawer with Close / Correct / Forget incl. profile self-correct; timeline kept as a tab), "Remember this" on every message (private threads choose audience) + `/remember` shortcut + toast "Open in memory", coverage chip on Scout answers (`--text-3` / `--warn-text` / `--danger-text`, absent when unstamped), work_result → Work deep link, composer "Attach from Drive" with `contextRefs` chips and 403 restore; `frontend_memory_wave8_test.go` (4 pins).
Scope: remember() seam, memory inspector + correction, person model from positions/decisions + accounts roster, decay above transcripts, chat-native extractor, anchor spill, coverage on answers, explicit private remember, work→ledger, recall budget by class, digest truncation recovery, Scout company-source lock fix (+ OPS-5).
Backend done: `memory_remember.go` (`POST /assistant/remember` + `remember_note` tool), `memory_inspector.go` (`GET /assistant/memory/inspect`, `POST …/inspect/action`), `channel_digest.go`, `work_result_ledger.go`, `recall_budget.go`, `recall_answer_coverage.go`; `memory.go` RWMutex + in-place search; taste analyst fed by decisions/positions with the accounts roster; `BRAIN_DECAY_DAYS`; ledger spill; truncation retry at 6000 tokens + `Stuck` on the record. Reviewer doubts: temporal budget vs gold pin; inspector `correct` semantics; only org-public channels digested.

## Wave 9
Status: `completed` — batch gate 2026-09-02 (recall reviews + fixes + full suite + e2e, see "Batch gate" and "End-to-end verify" below); prior note: (backend done within doctrine and reviewed — board/suggestion seats hard-pinned with no down-tier twin; frontend D3 done 2026-09-02: `#statusPill` is a button with five honest states (ready · backup model · paused · limited · offline), idle reads as ready, lane popover `role=dialog`, 60 s visible-only poll, `frontend_scout_status_wave9_test.go`; reviewed — two fixes applied: `aria-live` text written only on change, popover keeps Tab-to-close; Claude fallback (D2) still founder-gated)
Scope: per-seat fallback + provider breaker, readyz "fallback active", extraction workers behind the breaker.
Backend done: `provider_breaker.go` (closed/open/half-open per provider+seat, quota opens immediately, twin-dial replay luna↔terra, injectable clock), `withProviderResilience` on `createOpenAITextResponse`, router/answer provenance, ambient pause on open breaker, `/readyz` lane states idle|healthy|degraded|fallback_active|paused_by_breaker|disabled with request/success/failure stamps; `idle` no longer counts as degraded. `scoutFallbackProvider()` seam returns none (doctrine).

## Wave 10
Status: `done` — closing evidence in "Polish pass — closing evidence" below; awaiting AJ ratification of the §1.2a light pass (was: polish agent running D1–D3 + four AJ additions: rich-media feed canon — X posts / articles / YouTube / images "should all look amazing"; the nav pass ("the nav isn't there"); light-mode palette re-tune as a ratifiable token proposal; settings audit — every setting needed and clear)

### Wave 10 D3 — acceptance against the pinned goal and the founder's bar (2026-09-02)

| Clause | Evidence |
|---|---|
| "wave plan committed under docs/plans/" | `docs/plans/stride-v2-evolution-{plan,execution-plan,execution-log}.md` — committed with Wave 1 (`e448f2db`), extended and committed with this batch |
| "every wave shipped this session passes /code-review" | Go recall review (5 angles → 40 → 27 kept → 10 reported, all fixed, fix-review pass); frontend recall review (4 angles → 32 → 23 kept → 10 reported, all fixed); both ReportFindings calls carry `fixed` outcomes; tests per fix in `code_review_fixes_2026_09_test.go`, `drive_review_fixes_test.go`, `frontend_{work_hub,drive_memory,rooms}_fixes_test.go` |
| "… + end-to-end verify" | Wave 1: prod probe; Wave 6: two-participant Chrome QA + headless harness; Wave 10: real-link feed, budgets, phone shots, GIF, journeys; Waves 3/4/5/7/8/9: "End-to-end verify (critic round 1)" section below |
| "committed to main with the goal clauses evidenced" | batch commit `4f15435e` on main (this table is the evidence index) |
| five surfaces (rooms, chat, work, docs+decks, drive) | Waves 6–7, 1, 3, 4, 5 sections above; pins `frontend_rooms_wave{6,7}_test.go`, `frontend_conversations_wave1_test.go`, `frontend_work_hub_wave3_test.go`, `frontend_studios_wave4_test.go`, `frontend_drive_wave5_test.go` |
| compounding intelligence (analyze, learn, discard, recall) | Wave 8 D1–D12 (remember seam, inspector, person model, decay, chat-native extractor, coverage, budgets, digest recovery, source-lock root cause) + Wave 9 breaker; pins `frontend_memory_wave8_test.go`, `channel_digest_test.go`, `channel_ingestion_supersession_test.go`, `provider_breaker_test.go` |
| beautiful / intuitive / modern / snappy / elegant | Wave 2 canon + Wave 10 polish (light re-tune RATIFIED, dark ladder v2 RATIFIED, dark default, consistency sweep) + shell (wordmark, no flame/date/status, bell top-right, theme in Settings) + Home v2 + twelve motion plans DONE; budgets: cold interactive p50 110 ms, route first-paint p50 13–14 ms, 0 long tasks; pins `frontend_design_system_v2_test.go`, `frontend_polish_wave10_test.go`, `frontend_home_v2_test.go`, `frontend_no_edge_bars_test.go` |
| dark default + consistency end-to-end | `TestIndexThemeDefaultsToDark`; dark-first sweep list in the polish closing evidence; taste calls in memory `aj-design-taste-signals-2026-09-02` all honoured (accent bars: settings nav bar removed in critic round 1) |
Scope: integration design pass, snappiness budgets, final acceptance (+ OPS-6).
AJ direction 2026-09-02 (in flight, dedicated agents): (1) the rail's aperture mark becomes a Bonfire org-avatar tile with a two-tone flame that works on both themes ("no random marks like that left thing no one knows what it is"); (2) the active rail tab keeps its ember glow and loses the thick curved left accent bar ("a mark of AI design"); (3) rail + topbar become one seamless glass chrome, liquid-glass inspired, no leftover badge letter; (4) the rail's bottom utilities (bell / theme / account) align on one axis; (5) the depth ladder must read identically in dark and light; (6) the chat header "Riff" pill becomes a Scout button (flame glyph, user-facing copy "Scout · private"; identifiers and `data-icon="riff"` pins kept).
Done inline 2026-09-02: the organization chip never reads "Organization unavailable" — the workspace is single-organization (Bonfire, creation closed), so `/auth/me` now carries `organization` (authority store's single active org → `STRIDE_ORGANIZATION_NAME` → "Bonfire") and the topbar switcher, phone subtitle and settings card fall back to it (`workspaceOrganizationLabel()`; pins `identity_organization_test.go`, `frontend_organization_label_test.go`). Cause on the sandbox: the identity surface answers 401 without the E10 runtime; prod answers Bonfire for AJ.

### Batch gate — Go recall review of the OPS-2…6 batch (2026-09-02)
Five finder angles (Drive/Studios/Work, Rooms, memory/breaker/chat, removed-behaviour, cleanup) → 40 candidates → four batched verifiers → 27 kept (18 CONFIRMED, 9 PLAUSIBLE), 10 reported via ReportFindings. Top confirmed: the scheduled blob sweep never walks `deckSceneRef`/version `SceneRef` (would delete every deck scene a week after deploy); an open breaker replays quota failures onto the twin dial; inspector close/correct republishes private records org-wide; file websocket events fan private rows to every member; Drive import stamps organization visibility; tombstoned messages survive in the channel digest; the guest bucket drops screen-share teardown; host remove is not durable; ICS lacks SEQUENCE/ORGANIZER/cancel; trashing the newest version hides the whole chain. Refuted: attachment grants after soft delete (re-resolved through the trash-refusing source), 409 cascade on recording upload (client stops on first refusal), relabel-before-renegotiate race (client heuristic + adopt path), recall guard calibration, screen m-line codec pin (covered by `sfu_screen_share_test.go`), disabled/idle bucket (unreachable), taste roster cost (gated by unconsumed count). Fixes dispatched to three devs (Drive/blobs, Rooms/around-call, memory/breaker/health) with a test per fix; re-verify before the critic loop.
Fixes landed 2026-09-02 (one test per finding, `go build` green): Drive/blobs — walker keeps `deckSceneRef`/`renderSourceSceneRef`/`renderPdfAssetRef`/asset `SourceSceneRef`/version `SceneRef`, fail-safe refuses deletion when the walk yields no refs (`TestBlobSweepKeepsDeckSceneRefsAndVersionSceneRefs`, `TestBlobSweepRefusesWhenReferenceWalkYieldsNoRefs`); import inherits the source fence (`TestDocumentImportInheritsSourceFileAccess`); `reconcileFileVersionChain` re-heads on trash/restore/purge; PATCH validates before applying; restore requires a readable row; stamp failure removes the appended row; dead `blobOrganizationVisible` deleted; `entriesOfKindByMetadata` for blob auth; one usage scan + `deleteEntriesByIDJournaled`; per-recipient `file` websocket projection (`TestAssistantFileBroadcastProjectsRowPerRecipient`). Memory/breaker/health — open breaker fails fast on quota (`providerBreakerOpenError`), epoch-guarded success, inspector events inherit the source fence, taste cursor = newest consumed decision, `earliestVisibleCreatedAt()` under RLock, channel digest behind the meeting-digest seat + `channelDigest` lane, tombstone/supersede mark the digest stale and a `drainedWork` hook rebuilds from live rows, notes inherit the member-restricted fence, digest fence from current membership, remember tool off the room voice list, single-scan backfill (now `go`-launched off the boot path, main.go), stale+live lanes degrade, `paused_by_breaker` counts as degraded, disabled accounts skipped in mention directory/resolver/delivery. Rooms — share teardown never rate-limited, hand raise no-op + per-session bucket, sitting-scoped ejection record honoured at admission, ICS `SEQUENCE`/`ORGANIZER`/quoted CN/`METHOD:CANCEL`, 409 carries `partCount`, unresolvable seated participant fails closed, persist-before-refuse, host-mute stamp cleared on new session. Open: `TestWebsocketRoomModerateMuteAsksTargetThenEnforces` (fake track session vs session-bound enforcement) being fixed.
Fix review (feature-dev reviewer over the fix diffs, 2026-09-02): everything consistent with its tests; one gap — the reply-notification branch in `notifyScoutChatTargets` lacked the disabled-account fence the mention path has → guarded + `TestChatReplyNotificationSkipsDisabledAuthor`. Noted, not a defect: `blobAuthorized`'s chat-image branch grants on thread visibility (images are minted into the thread; that is the contract, unlike Drive-file attachments).
Shell chrome landed (nav-designer, 2026-09-02): active-tab accent bar removed (`box-shadow:none`, glow only); `#brandMark` is a 36 px r10 org-avatar button with the Bonfire flame (one cubic silhouette, compact `#FFB36B→#FFE3C2` core, 50% ember radial glow) on `--ink-700` light / `--ink-600` dark with an 8% inner hairline, hover lift + specular, focus/expanded/listening rings; the organization chooser menu moved into the rail under the tile (topbar chooser text-only, badge letter gone); rail takes `--glass-chrome-fill/-filter`, transparent right border, one continuous hairline with the topbar; utilities share one axis and one hover well; pins re-pinned in `frontend_latency_test.go` ×4 and `frontend_pd1_top_level_ia_test.go`; canon note in the design doc; shots `scratchpad/nav/{light,dark}-{corner,shell}.png`. To ratify: the flame; dark depth is hairline-only on the near-black ramp — re-tuning the dark surface tokens is AJ's call. Conversation header (riff-rename agent): right-side meta hidden (kept as title tooltip), Scout pill + bell right-aligned at one 36 px height across channel/private/group; the flame family also replaced the two-line glyph on the checkpoint eyebrow and the message hover action.
Frontend recall review of the batch (`index.html`, 2026-09-02): four finder angles (Work/Studios; Drive/memory/status; Rooms in-call + around-call; design-system/menus/removed-behaviour) → 32 candidates → four verifiers → 23 kept (W: 5 confirmed, 1 plausible, 3 refuted; V: 8 confirmed; R: 6 confirmed, 1 plausible, 1 refuted; M: in verification), 10 reported via ReportFindings: print-in-Source-mode data loss, un-reviewed deck versions presentable, identity switch keeping the prior account's Work list, leave-while-sharing never sending `screen_share_stopped`, Copy-link minting a new token per retry, checkpoint double submit, rAF-gated meeting-record jump, `/remember` clearing the composer before save, memory-drawer focus/Esc stranding, room lock unreachable on phones. Refuted: ask-for-changes empty id (server always sets `RootArtifactID`), ⌘S-during-autosave rejection path, TDZ on auth apply (all callers post-declaration behind awaits), reaction-row hand state (aria-label swap announces it). Three frontend fix devs dispatched with a pin per fix.
Design-system/menu set verified (M): confirmed — Drive share-expiry menu built on a detached trigger (never in the DOM), upload-visibility menu positioned against `.workspace` (off-screen), the two adopted chat menus lingering 220 ms and swallowing clicks, the header pill flame 14% larger than `scoutFlameMark()`, the green-room divider on a fill token; plausible — bfMenu fixed-branch `transform-origin` read on the starting-style flush; refuted — upload-visibility menu left open on Trash (light dismiss closes it), kebab re-adopt stealing search focus (light dismiss closes first). Dispatched to the Drive and chat fix devs.
First full `go test` on the mid-edit batch tree (937 s): 14 red — settled as: voice-tools exclusion pin re-pinned to −3 (remember tool excluded, intended); sfu screen-share stop broadcast → rooms dev; three `padding-left: 56px` shell pins + PD1 rendered nav + UX-audit raw-colour count → shell designer (wide labelled rail is the new geometry; token it); settings relabel copy + rich-preview reserved-height literals + sports-car rendered journey → polish; Work rendered contract → Work fix dev; media-fix node verification → rooms fix dev. `TestDriveStoreEntriesOfKindByMetadataClones` and `TestReadinessScoutLanes…` passed on rerun (mid-edit transients).
Shell org-first landed (2026-09-02): `#topbarOrganizationSwitcher` moved into the rail as the top row (flame tile `#brandMark` + name + status + chevron, one bfMenu trigger, Esc returns focus); Stride wordmark + lockup removed from the topbar (wordmark tokens untouched); `#appShell[data-org-identity="lockup|subtle"]`, default `subtle` (Buzz-style: tile alone top-left with tooltip; account row "AJ" / "Bonfire · status" bottom-left); phones unchanged; re-pins: latency ×4 (`brandMark` span), `padding-left: var(--rail-width, 56px)` ×3, PD1 `railWidth:168` + org child count, shell_identity inverted (+ phone fallback), chat_quality/brand_assets inverted, icon_system wordmark line dropped, ux_audit raw-colour count 2→3 (video-tile quality badge is a genuine dark-media exception). Ratifiable "dark depth ladder v2" block in the design doc (`--surface-1/2/3` → `#0E0E10/#151518/#1C1C20`, chrome `rgba(14,14,16,.86)`, lines .07/.13; contrast ≥4.5) with before/after shots `scratchpad/nav/dark-ladder-{before,after}.png`. Chat menus now fade + stop hit-testing on close (`TestDesktopChatMenusFadeAndStopHitTestingOnClose`), header pill flame matched to `scoutFlameMark()` size, green-room divider on `--line`, bfMenu fixed-branch origin from written left/top + offset size.
AJ authorization (2026-09-02, verbatim): "you have full permission to take waves 2-10 and handle any migrations needed and get it on main and live on vps when ready." Motion pass rolled INTO Wave 10 at AJ's request (improve-animations audit running; plans → devs → this batch).
Motion pass rolled into Wave 10 (AJ, 2026-09-02): `improve-animations` audit — four category agents over the batch tree → 44 findings, all re-vetted by literal → 12 plans `plans/009`–`020` (chat message entrance to token speed on every breakpoint; rail icon bounce removed from both navs; one `--press-scale`; toast/pop-in/sheet recipes on tokens; hover + keystroke feedback on `--dur-fast` + token residue; message hover actions composited; five one-way panels made reversible; proposal cards keyed entrance; bfMenu origin on every branch; composite-only sweep across PiP drag / voice bars / transfer panel / voice island / resize grip / receipt fill; reduced-motion completeness incl. `--dur-breathe`, transcription-dot specificity, five ungated smooth scrolls, GIF stills, grill counter cancel; continuity moments — thread-list FLIP, work-step transitions, theme crossfade, previewer direction, move attribution with undo). Two dev lanes executing in parallel (A: 010→011→012→013→009→014; B: 017→015→016→018→019→020); each plan gated on the `TestIndex|TestPolishWave10|DesignSystem` battery.
AJ shell decision (2026-09-02, after seeing both variants): no flame tile; the `stride` wordmark returns to the rail top-left aligned with the icon column; no date in the topbar; account row "AJ" / "Bonfire" with no status; status pill stays top-right; both `data-org-identity` variants flame-free. Landing via `shell-final`.
AJ ratified the Wave 10 light-mode re-tune (2026-09-02: "the light re-tune is nice we can do that") — design doc §1.2a flipped from PROPOSAL to RATIFIED; values unchanged from the shipped proposal. Still open for AJ: dark depth ladder v2 tokens.
Motion lane A DONE (plans 010–014 + 009): rail icon bounce removed; 83 presses on `--press-scale` (+4 `scale:`-property presses → `transform`); toast/pop-in/sheet recipes on tokens (`bf-popin` scale 0.94→0.96); hover/keystroke on `--dur-fast` + token residue folded (`lobby-shake` → `--dur-med`); chat entrance composited 6 px/220 ms on every breakpoint; message hover actions opacity-only with a discrete width step. Pins re-pinned in desktop/mobile chat quality, device transfer, packaging stage drawer, private riff, design system, latency ×2, drive wave5, polish wave10. Lane B (017→020) in progress.
Ops: the stray public probe `scout-chat-message-951524182369b6c1e58665f6` in Bonfire Chat was deleted 2026-09-02 through AJ's own session on his explicit instruction (`DELETE /assistant/chat-threads/scout-chat-1785265483794248034/messages/…` → 200; thread preview reverted to the prior message).
Shell FINAL landed (AJ ratified, 2026-09-02): `#brandMark` is the `stride` wordmark (ratified PNG via `--wordmark-image`, `color: var(--wordmark)`), 83×22 on the wide rail / 36×9.5 slim, left edge on the nav glyph column (probed), still the org-menu trigger; flame tile + CSS deleted (`scoutFlameMark()` stays for Scout in chat); `#topbarDate` + separator + `headerDateLabel()` output removed; account row "AJ" / "Bonfire"; `data-org-identity` kept flame-free, default `subtle`; dark depth ladder v2 applied (`--surface-1/2/3` `#0E0E10/#151518/#1C1C20`, `--glass-chrome rgba(14,14,16,.86)`, lines .07/.13, rail on `--surface-1`), doc RATIFIED. Pins: shell_identity (+positive wordmark, flame/date/status forbidden), icon_system, brand test `color: var(--wordmark)` 5→4 (was already red at 3 on HEAD), theme/polish/design-system dark hexes → ladder v2. Shots `scratchpad/nav/final-*.png`. `npm run test:brand` 20/23 — the 3 failures pre-existing (nested stride-site repo, mobile BrandMark, theme-color meta).
AJ shell refinements (2026-09-02, in flight via `shell-final`): the top-right Scout status pill shows ONLY in non-ready states (backup model / paused / limited / unavailable) and never says "offline" ("you're on the app, it'll never be offline by nature"); the notification bell moves to the topbar right; light/dark/system moves to Settings → Appearance; the rail bottom holds only the account row; a Settings → Scout section carries the full lane list. Home v2 (AJ: "if you had other ideas there go for it") in flight via `home-v2`: greeting + composer unchanged; three quiet sections below — today (upcoming meetings, live-now row), since you were away (unread mentions/groups, finished work, decision notifications; max three rows), continue (last three) — sections render nothing when empty; Packaging tiles + "what Scout learned" come with Wave 11.
Motion lane B DONE (017/015/016/018/019/020): bfMenu origins on every branch (+ CSS origins on four menus); notification panel + goalcard fully reversible (memory body / Drive details / room chat entrance-only — hosts hidden synchronously by JS, follow-up noted in plan 015); proposal cards keyed entrance (`TestIndexProposalCardEntranceKeyedOnNewIds`); composite-only sweep (PiP `translate`, voice bars `scaleY`, no filter tween, island ring on `::after`, grip `scale`, receipt `translateX`; grep gates 0); reduced motion complete (`--dur-breathe: 0ms`, scroll gates, GIF stills + tap-to-play, `pulse` defined, grill counter cancel); continuity moments (thread-list FLIP, step transitions, theme crossfade, previewer decode + glide, move ghost + undo; `TestPolishWave10ContinuityMoments`). All twelve motion plans DONE; `plans/README.md` updated.
Shell refinement landed (bell top-right `.topbar__notify`, `#statusPill` only when Scout is not ready with copy never "offline", Settings → Scout lanes `#settingsScoutLanes`, theme in Settings → Appearance with `#themeToggle` on the fieldset, rail bottom = account row; pins re-pinned in latency/PD1/scout-status/polish/shell-identity; shots `scratchpad/nav/final2-*.png`). Home v2 landed (today / since you were away / continue; `frontend_home_v2_test.go` ×4; shots `scratchpad/home/home-v2-*.png`; judgement: `home.lastSeenAt` stamps only on in-app Home unmount). Design-system tier pins re-anchored to line-start selectors (plan-020 theme-crossfade `:is()` rule precedes the tier block). Settled-tree gate started: full suite + goal-anchored critic.
AJ ratified the ember wordmark on dark (2026-09-02: "orange is cool on dark mode"); doc + memory updated; no token change.
Full suite on the settled tree (2026-09-02, `scratchpad/full-batch-2.txt`, 895 s): 3 red → `TestDocumentStudioRenderedNestedListsAndSafeImagesRoundTrip` was a real gap (the harness asserted light-theme hairlines while dark is now the default → harness pins `bonfire.theme.v1=light` via `addInitScript`; green), `TestRoomsFixesRenderedHostMenuDisposalAndMoreLock` and `TestReadinessScoutLanesReportIdleNotDegradedWithHonestShape` pass alone (load flakes under the full run; both rendered/timing-bound). Earlier closures logged here for the critic: `TestWebsocketRoomModerateMuteAsksTargetThenEnforces` fixed by the rooms dev (test registered its fake track under the asked session; un-asked control track asserted NOT dropped); `TestDesignSystemV2GlassTiers` re-anchored to line-start selectors after plan 020's theme-crossfade `:is()` rule.
End-to-end verify (critic round 1) outcome: Waves 3, 4, 5, 8, 9 PASS; Wave 7 API PASS (ICS SEQUENCE/ORGANIZER/CANCEL, recording default off → on) but the first recording upload from a member alone in the room returned 403 "recording consent could not be verified" — the R6 fail-closed change refused the recorder's OWN seat when the roster name did not resolve; FIXED: root cause was the runtime consent authority built with a nil store when Postgres is absent (`Health()` failed → every lane check errored → "could not be verified"); `meetingRecordingConsentDecision` now takes the uploader — the recorder's own seat consents by the act of recording via its durable anchor (session email, never the roster name, no lane-authority call), other seats resolve via the admitted account behind their live socket (`seatedMemberAccountEmailsInRoom`; prod display-name ≠ email) then roster, unverifiable others still fail closed; tests `TestMeetingRecordingConsentAcceptsRecorderOwnSeat` + refusing-other; Wave 7 e2e re-run on the rebuilt sandbox: first upload 200, recording saved (400 KB), zero page errors → PASS.
Critic loop (goal-anchored, threshold 9.0 / floor 7.0 / 2 rounds): round 1 REJECT 7.3 (goal-clause evidence 6, surfaces 8, design 6, motion 9, gates 8, ship-readiness 7) → nine revisions applied (settings-nav accent bar + two Drive edge stripes removed with `TestIndexNoActiveEdgeBars`; wordmark canon reconciled and ratified orange-on-dark; doc prose landed; e2e for Waves 3/4/5/7/8/9 logged, Wave 7 recording defect root-caused; full suite logged; Wave statuses + D3 acceptance table; Ops tracker filled; dormant "offline" tag removed). Round 2 REJECT 8.7 (9/9/9/9/7/9) — single remaining item: `TestRoomsFixesRenderedHostMenuDisposalAndMoreLock` fails 2/10 alone (host-menu programmatic open must be focusable synchronously, or the harness races layout) → SETTLED: the false half was `isOpen()` because the harness's bare `<video>` tile was disposed by the app's dead-tile reaper (`pruneDeadRemoteVideoTiles`, `readyState≠'ended'`) — product contract holds (menus never self-close on focus moves; no `index.html` change); harness now seeds a live `canvas.captureStream()` track and waits on `menu.hidden===false` + a layout read; 10/10 alone. The earlier full-suite classification of this test as a load flake is withdrawn — it was a harness seeding defect. Max rounds reached: after that item is settled the batch proceeds under the force-accept protocol with the round-2 scores attached (`forceAccept: true`, remaining gap = this test's classification), per critic-loop rules.
Main also received two fixes from the pasted task prompts: `c1f12ab7` (archive-close flake joined the lifecycle mutex) and `e02bbc73` (rooms handler tests self-contained); the batch commit rebases onto them and merges hotfix `c1fdd9da`.

### Polish pass — closing evidence (polish lead, 2026-09-02)
Sandbox: keyless `ma -addr :3171` in `scratchpad/sandbox` (index.html is a symlink → restart after edits); HEAD's index on `:3172` from the same binary/data for before/after. Evidence: `scratchpad/polish/` — scripts `lib/feed/perf/phone/palette/gif/probe/darkdefault.mjs`, PNGs, `*-metrics.json`, `perf.json`, `probe-dark.json`. Posting only ever went to a members-only group `scout-chat-1788351789882428000` (AJ + Joel; the private Scout thread answers every link with an "I couldn't answer safely" bubble on the keyless sandbox).

1. **Rich-media feed** — `feed-light.png` / `feed-dark.png` / `feed-light-playing.png` / `feed-metrics.json`. Five live links: X post `x.com/jack/status/20` (author, handle, avatar, text, date from the keyless publish.twitter oEmbed; shrink-wrapped 227×143, max 640), a Cloudflare blog article (16:9 OG image, favicon-fallback initial, 520×417), YouTube (16:9 poster, brand chip, play glyph; Enter/click mounts the `youtube-nocookie` player with `allow="autoplay; encrypted-media; picture-in-picture; fullscreen"` and "open on YouTube ↗"), a bare JPEG (natural 3:2 capped at 420, 520×422), a GIPHY GIF (GIF tag, 480×365). CLS during media decode **0.000**; card heights identical at first metadata render and after decode (`layoutShiftedCards: []`); the 0.146 recorded earlier is thread hydration (message nodes appending) before any card exists. Keyboard: Enter on the article opens one new tab (`rel="noreferrer noopener"`), `--ring` focus outline visible. Dark: `--surface-2` fill, `--line` hairline, `--well` media box. **Why the predecessor saw empty previews:** bad fixtures, not the server — the Wikimedia `/thumb/…/640px-…` path answers 400 even to a browser (full-res `/commons/a/a9/Example.jpg` proxies fine with the current UA) and the X status id was fabricated. X / YouTube / TikTok are keyless oEmbed lanes and work on the sandbox; only GIPHY search needs `GIPHY_API_KEY`.
2. **Snappiness (D2)** — `perf.json`, method in the script header. 5 runs on this Mac against localhost (floors, not prod numbers): cold `/` → interactive shell (authed, nav present, route functions live) **p50 110 ms / p95 117 ms**; FCP ≈ DCL p50 94 ms; TTFB 0.6 ms; 4.4 MB decoded HTML. Route first paint (double-rAF after `selectPD1Destination`) **p50 13–14 ms, p95 ≤ 18 ms** on all five hops; 0 long tasks. Budgets to hold: cold interactive < 600 ms on prod data (plan), route switch < 100 ms. Not test-pinned (shared laptop).
3. **Phone 390×844 @2x** — `phone-conversations-*.png`, `phone-thread-*.png`, `phone-thread-media-*.png`, `phone-settings-*.png`, `phone-metrics.json`, light and dark. No horizontal overflow on any surface (docW 390); cards 273 px wide with their aspects; composer 706–752 px, tab bar 774–822 px (a 22 px seam — the "gap" I first saw was my script's `scrollIntoView` dragging the whole column, fixed in the script). Fixed in product: the phone header subtitle read "public · everyone" over a members-only group (`phoneToolSubtitle`) → "group · N people". Noted, not fixed (shell): `#chatTool` is 12 px taller than the viewport on phones, hidden under the fixed tab bar.
4. **GIF picker (D4)** — `gif-picker-unconfigured-light.png`, `gif-picker-{light,dark}.png`, `gif-sent-light.png`, `phone-gif-picker-{light,dark}.png`, `gif-metrics.json`. `#scoutChatGif` in the composer's attach chrome (mono "GIF" glyph, 44 px hit) opens `#scoutGifPopover`, a glass-float sheet: search (220 ms debounce) → `/assistant/giphy/search?q=&limit=24` (trending when empty); a 3-column fixed 1:1 grid (143 px cells, cover-fit) so results never reflow; roving arrows (Right/Left/Down/Up, Home/End, Up from row one returns to the search field); Escape returns focus to the button; outside click closes; never stacks with the deliverables drawer. A pick → `POST /assistant/giphy/import` (keyless, giphy.com-allowlisted, byte-validated into the attachment store) → the returned file rides `pendingScoutFiles` + `sendScoutChat`, so the feed renders it with the same inline GIF frame as any upload (384×480 blob; blob route re-verified 200 after hydration). Without `GIPHY_API_KEY` the sheet says "GIF search is not set up on this server — set GIPHY_API_KEY to turn it on. Pasting a GIF link still works." (live 503 on the sandbox). Phones: a fourth affordance left the input 78 px wide, so ≤ 480 px the button hides and the picker lives in the attach menu as "GIF from GIPHY" (input back to 126 px). Search grid exercised with the route stubbed on real GIPHY CDN ids (no key here); import exercised live. Pin: `TestPolishWave10GifPicker`. Prod needs `GIPHY_API_KEY` (README).
5. **Light palette, before/after** — `palette-side-by-side-{lobby,conversations}.png` (before = HEAD index on :3172, same binary and data; after = the tree; the same private Scout thread on both sides). Token deltas as §1.2a: `--surface-2` #F2EDE4 → #F9F6F0 (`--paper-25`), shadows 14,14,16 → 38,35,30, lobby on shared tokens. **For AJ to ratify.**
6. **Dark default (AJ mandate)** — pre-paint script, `storedThemePreference()` and the `theme-color` meta fall back to `dark`; a saved `light` still wins; `/auth/me` `themePref` untouched (empty → client default). Verified signed out with OS light: `data-theme="dark"`, meta `#000000`, black ground (`signed-out-dark-default.png`); stored `light` → light. Pin `TestIndexThemeDefaultsToDark` replaces `…ToLight`; design doc §1.2b.
7. **Dark-first consistency sweep** — `probe.mjs` tallies computed fills / hairlines / radii / control heights / shadows / hover fills on Home, Conversations, the attach menu, the GIF sheet, Rooms lobby, Work, Drive and Settings in dark (`probe-dark.json`, `probe-*-dark.png`). Every one-off normalised:
   - tokens: dark `--shadow-1/2/3` and `--glow-accent` were the light ink shadows (rgba(38,35,30,…), invisible on black) → black at .40/.45/.60, so the studio workspace, lobby rail/panel, file tiles, drive segments and settings nav lift in dark for the first time;
   - radii → ladder: `.settings-nav__item` 11 → `--r-md`; `.drive-nav__item` 11 → `--r-md`; `.drive-new` 14 → `--r-md`; `.drive-heading__action` 10 → `--r-md`; `.drive-sidebar__brand` icon 11 → `--r-md`; `#chatTool .chat-thread-item` 14 → `--r-md`; `.chat-threads__new--inline` 9 → `--r-sm`; `#chatTool .chat-threads__search` 12 → `--r-md`; `#chatTool .scout-chat-form` 22 → `--r-xl`; `.studio-projects__workspace` 28 → `--r-xl`; `.studio-projects__search` 14 → `--r-md`; `.file-tile` 16 → `--r-lg`; `.file-tile__preview` 12 → `--r-md`; `.lobby__new` 12 → `--r-md`; `.room-empty__join` 14 → `--r-md`;
   - hairlines → tokens: `.file-tile__preview` inset white 8 % → `--line-1`; `.room-empty .greenroom__tile` white 14 % → `--line-strong`; `#chatTool .chat-threads__search` / `.chat-thread-item[aria-pressed]` / `.scout-chat-form` dark rings (white 8 / 10 / 11 %) → `--line-1`, their `[data-theme="dark"]` overrides deleted (one theme-aware rule each);
   - lifts → ladder: `.file-tile` black .035 → `--shadow-1`; `.settings-nav__item[aria-current]` black .08 → `--shadow-1`; `#chatTool .chat-thread-item[aria-pressed]` and `.chat-threads__search` three-stop blacks → `0 0 0 1px var(--line-1), var(--shadow-1)`; `#chatTool .scout-chat-form` → `…, var(--shadow-2)`;
   - control heights: nav rows 42 / 38 → 40 (`.settings-nav__item`, `.drive-nav__item`); toolbar controls 42 → 40 (`.drive-new`, `.lobby__new`, `.studio-projects__search`);
   - hover wells: `.chat-thread-item:hover` `--accent-soft` → `--well`; `.home-continuity__row:hover` surface-2/66 % → `--well` (rows hover to the well everywhere; menus, settings and drive already did).
   Deliberately left: `.lobby__panel` `--r-2xl` (ratified mock; the dialog/PiP token); `.drive-new` border `--glass-border` (a glass-tier token); rail and primary-nav hover `--accent-soft` (the shell designer's); `.room-empty__join` 50 px (the one hero CTA); segmented/chip controls 26–32 px (chips, not buttons); form fields 44 px vs toolbar 34/40 (form vs toolbar). Re-probe after: radii on Conversations / Drive / Settings collapse to {8, 12, 16, 22, 999}, hairlines to {.09, .16}, every dark shadow black-based.
8. **Suite failures settled (coordinator's three):** (1) `TestIndexRelabelsModesAndDefaultsOnDesktop` re-pinned with the settings pass's copy — voice focus now says what it removes ("removes keyboard, fan and room noise … nothing to set up") and raw mic when to reach for it ("nothing applied except echo cancellation. For instruments, or when cleanup sounds wrong") — clearer than the mechanism copy it replaced; (2) the reserved heights are back in the card system rather than re-pinned: `.is-loading { min-height: 168px }` (the skeleton reserves a card before metadata lands), `.is-text-only { min-height: 96px }`, the reply-context thumbnail `aspect-ratio: 16 / 10`, and `card.dataset.kind = previewKind` with the image override on its own line (Wave 10 pin updated to the same shape); (3) `TestWebChatSportsCarRenderedJourney` was tripping on neither the header, the cards nor the composer — on two keyboard regressions from the Wave 2 D4 `bfMenu` adoption of the reaction picker: (a) `bfMenu.open()` moved focus into the menu only in a `setTimeout(0)`, so Enter → ArrowRight with no pause landed on the *first* item (👍 instead of ❤️); it now focuses synchronously and keeps the macrotask as the fallback (the pinned literal stays); (b) `restoreDesktopChatContextViewport` re-focused "the same control" by emoji key and picked the reply card's summary button instead of the picker item, tearing focus out of the picker mid-pick so its restore-to-trigger had nothing to return to; it now restores the exact node (patches are in place) with a summary-parity keyed fallback. The journey passes against the tree; it also passes against HEAD's index, so the regression was isolated to the tree. Trap: the rendered journeys wait on 450 ms fixture delays — two `go test` runs overlapping made one flake; run them alone.

Verification: `go test -run 'TestPolishWave10|TestIndexTheme|TestIndexRelabels|TestDesktopChatRichPreviews|TestDesktopChatQuality|TestDesignSystemV2|TestWebChatSportsCarRenderedJourney|TestDesktopChatMenus|TestDesktopMessageOwnership'` green; the broad static sweep (`TestIndex|TestDesktop|TestDesign|TestPolish|TestFrontend|TestChat|TestDrive|TestSettings|TestLobby|TestRooms|…`) green (one contention flake, `TestIndexRoomsWave7…`, passes alone). The `more`-dots glyph pin was red on the tree at start (the arcs had reverted); the coordinator restored it. Full suite: the batch gate's.

Open / for AJ: ratify the §1.2a light pass (item 5) and the dark default (item 6, mandated, now in the tree); `GIPHY_API_KEY` for prod; TikTok / Instagram cards were not re-exercised in this pass (the predecessor's `w10-shots-cards/` holds a live 9:16 TikTok card); the settings audit stays the predecessor's D-item (copy re-pinned, structure unchanged).

## Wave 11
Status: `in_progress` (started 2026-09-02 after the Waves 2–10 batch commit `4f15435e`/`722f5cf6`, in parallel with the OPS-6 activation from a separate release worktree; three devs — backend A: `packaging_commissions.go`, `artifact_projects.go`, duplicate route, `report_print.go` branding; backend B: `packaging_intake.go` + `scout_followup_watcher.go`; frontend: Packaging Studio hub, three studios + brief sheets, deliverable actions, project picker, threaded clarifications, starter pills removed)
Scope: Work → Packaging Studio; New Presentation / New Document / Commission Research flows; Bonfire-branded research reports; rename / duplicate / save / export on every deliverable; project tags that auto-file into Drive folders; chat intake with clarifying questions as threaded replies; Scout follow-up watcher (reply / act / stay silent, reconciles multiple opinions, re-tag always forces) (+ OPS-7).

### Scout findings (2026-09-02, file:line at the batch-gate tree)
- Work hub: list route `studio_projects.go:1073` (`/api/studio-projects/v1`, GET list + PATCH rename/archive with `expectedRevision` CAS, `studioProjectStatusCanRename` :692); kinds closed at :21-32; a read projection over goal roots, no project record. Blank creates `studio_blank_create.go:73` (document) / :126 (deck) via `createDocumentStudioArtifact` :98. Chat → private work `conversation_work_launch.go:209` → `launchConversationStudioProcessOnce` :494; public work is proposal-gated (`conversation_public_work_launch.go:321/525/739`). Frontend: hub `index.html:39090` (`#researchTool`), installed-apps launcher :39107-39137 (`data-studio-app-kind` × `new|open|drive`), `createBlankStudioProject` :47886, `startStudioProjectWithScout` :47858 (prefills chat text), `renderStudioProjects` :47378, labels `toolTitles` :48254 (`research: 'Work'`), `studioProjectSubtitle` :48834, `studioProjectFilterTitles` :46800, nav :37961 `data-pd1-destination="Work"`.
- Research: no route — Scout agent-thread mode via the `deep_research` tool (`tool_registry.go:128`, contract `research_brief_v2`, rubric `research_brief_gate_v1`), model at `agent_thread_runner.go:2477` (`gpt-5.6-sol`/high); print sidecar `report_print.go:186` with branding HARDCODED "Stride" (:211 wordmark, :213 kicker, :235 colophon) — the seam for organization branding; PDF via `main.go:1779` (`:1857` converts research markdown to the print doc).
- Deck engine: `packaging_studio.go:267` definition (rev `v8.premium-design-contract.v1`); the brief = stage 1 `context_snapshot` fields :277-284 (direct_ask, audience, decision, desired_response, slide_count, taste_signals, brand_assets, research_mode, research_questions, known_facts…); imagery `slot: bleed|plate` (:375, at most ONE bleed :1507-1513, full-bleed law :432/:688) → "full-bleed" mode must lift the cap; themes `deck_theme.go:50/73/80/120`; research attaches through `research_mode: external` (:286-291) + `ContextRefs`.
- Documents: `document_report` process `process_definitions.go:955` (brief :988: direct_ask, reader, decision, intended_use, document_shape, scope, voice, constraints, research_mode…); editor `document_editor.go:117`; DOCX `document_docx.go:820`; save `files.go:3262` → `saveDeliverableSnapshotToFilesNamed` :3151.
- Rename/duplicate/tag: rename only via the studio PATCH; duplicate exists (`deck_editor.go:~330-366` `/artifacts/deck/copies`, `document_editor.go:202` `/artifacts/document/copies`, both take `folderId`+`fileName` and snapshot to Drive); tags/projects on artifacts: NONE (greenfield); folders `file_folders.go:196/200/257/496/612/616`, routes `main.go:1191-1192`; folder-by-name-or-create pattern to reuse: `kanban.go:6568-6589` (`saveToFilesTool`).
- Threads: `ReplyTo *scoutChatReplyRef` on messages (`scout_chat_threads.go:837`; ref type :570-580 with `MessageID` parent + `RootMessageID` hint); post via `/assistant/chat-threads/` (`main.go:1150`) with `replyToMessageId` (:73/:1879); ONE append helper `appendScoutChatThreadMessageWithReplyAndTool` :2791 (resolves ref :2851, `scoutChatReplyTargetsScout` :2861, ancestry backfill :3221); fan-out `chat_mentions.go:389` `notifyScoutChatTargets`; frontend `desktopChatReplyTopology` :79474, `syncDesktopReplySurfaces` :80817.
- Mention → Scout: lexical `parseChatMentionTokens` :24, `scoutChatMentionsScout` :140; work-ask detection `chatAgentWorkWords` :253 + `chatAgentExplicitWorkAction` :263 + `chatAgentWorkRequestIsBounded` :327 → `strideTargetedAgentWorkRequest` :348, called from the append path `scout_chat_threads.go:2913`. NO clarifying-question loop exists (definitions prefer labelled inference: `packaging_studio.go:280`, `process_definitions.go:988`); only checkpoint + `needs_input` (`studio_projects.go:39`).
- Ambient workers: `ambientAgentConfig` `agent_runner.go:451` (`providerSeat` :459, `cursorMetadataKey` :477, `inputFilter` :505, `drainedWork` :513, `roomScoped` :495); breaker pause :1054/:1139; copy `channel_digest.go:64` (`startChannelDigestWorker` :84); registration lists :1778/:1785; "since cursor" helpers `unconsumedEntriesAfter*` :1896-1921.
- Collisions: `index.html`, `files.go`, `main.go`, `studio_projects.go`, `conversation_work_launch.go`, `authorization_surfaces.go` (append rows at the END), `guest_allowlist_test.go` are all in the batch — Wave 11 starts after the batch commit.

### Prompt For Wave 11 (paste into a fresh session after OPS-6 is live)
You are the lead for **Wave 11: Packaging Studio (+ OPS-7)** of the STRIDE v2.0 evolution. Read `docs/plans/stride-v2-evolution-execution-plan.md` (Critical Rules + Wave 11 D1–D14) and this log's Wave 11 section (scout findings above). Founder intent is quoted in the plan; product decisions already made: the hub is **Research Desk + Presentation Studio + Story Studio** (documents are chat-harness work opened in the editor); commissions take structured briefs; chat is an intake (clarifying questions ONLY as threaded replies to the asking message); a Scout follow-up watcher decides reply / act / stay silent; project tags auto-file into `Projects/<name>`; research reports are organization-branded (`workspaceOrganizationName()`, `report_print.go` seam).
Team (omit `model` on every spawn): scout not needed (findings above); `backend-a` (general-purpose): `packaging_commissions.go` (`POST/GET /assistant/packaging/commissions`, brief validation per kind, launch through `launchConversationStudioProcessOnce`, research via the `deep_research` mode with `research_mode` + questions from the brief, presentation brief → `context_snapshot` + imagery mode lifting the one-bleed cap, story outline artifact `POST /assistant/packaging/stories` + versions + "build the deck"), `artifact_projects.go` (`PATCH /artifacts/project`, folder-by-name-or-create reusing `kanban.go:6568-6589`, move on retag), `POST /artifacts/duplicate` (wrap the existing copies), rename via the studio PATCH; org branding in `report_print.go` (name, wordmark per ground, ember hairline, "Prepared by Scout for <org> · date", cover, TOC, sources appendix) so DOCX/PDF match; three-registry rule for every route (append rows at the END of `authorization_surfaces.go`). `backend-b` (general-purpose): chat intake — in the append path after `strideTargetedAgentWorkRequest`, a work ask creates a commission with the brief pre-filled from message + thread; a `briefGaps()` pass decides the NECESSARY questions (kind, audience, sources, length, imagery mode) and posts ONE threaded reply (`appendScoutChatThreadMessageWithReplyAndTool` with `replyToMessageId`, mention the asker); answers in the thread complete the brief and start the run; `scout_followup_watcher.go` as an ambient worker (copy `channel_digest.go:64`; cadence 60–90 s; `providerSeat: seatChat`; cursor via `unconsumedEntriesAfter*`; per thread decide reply/act/silent with a bounded model call; multi-person reconciliation; fresh @scout always forces; journal each decision on the run log; one unsolicited reply per 10 min per thread; never reply to itself). `frontend` (general-purpose): rename to Packaging Studio everywhere (`toolTitles`, subtitles, nav label, mobile dock, ⌘ labels; ids stay), the three studio tiles + brief sheets (`.glass-sheet`, pills + open-ended + Drive attach + "just describe it"), deliverable actions on rows and in editors (Rename inline, Duplicate, Save to Drive, Download DOCX/PDF, Open in editor; research opens in the document editor), project picker + hub filter + Drive folder view, threaded clarifying questions with quick-answer pills, "waiting on <name>" row state, private-thread empty state = real actions only (D14). `reviewer` (feature-dev:code-reviewer) after each dev; then the recall `/code-review high` pass BEFORE the critic; critic-loop anchored to the founder's verbatim intent (threshold 9.0, floor 7.0, 2 rounds); end-to-end on the sandbox (a real research run when a key is present, else the deterministic fixture); rendered tests per flow; `go test -count=1 -timeout 40m .`; commit on main (fetch + rebase first, never checkout/reset/stash in the shared tree); OPS-7 via the exact-release ceremony with rooms empty; update this log (compact Wave 10, fill Wave 11) and memory.

## Wave 12
Status: `pending` (added 2026-09-02; starts after Wave 11)
Scope: streaming + tool progress, wide admitted tools with authority, inline step cards, budgeted turns; per-seat provider table (platform-chosen, never a user picker) with Fable 5.1 on design/judgment seats once `ANTHROPIC_API_KEY` is in the base env; eval bake-off before flipping the chat answer seat; provider stamps; Dissent acquired into STRIDE as a sub-product (Go port of contract/plan compiler/assurance/receipts/cost accounting behind the `workExecutor` seam) + founder-only DISSENT admin panel (token flow per provider/seat/model, cost, latency, breaker, assurance analytics, receipts, routing controls) (+ OPS-8). Dissent scout running 2026-09-02.

### Dissent port map (scout, 2026-09-02; repo `~/Documents/business`, TS, SQLite `data/frontier.db`, 156 vitest files)
Port to Go 1:1 (pure, deterministic): `src/crypto.ts` (canonical JSON + sha256 + HMAC-base64url — port FIRST with cross-language golden vectors; every receipt and self-authenticating `planId` depends on byte-identical canonicalization), `src/coordination.ts` (the `dissent.work.v1` contract: objective/context/acceptanceCriteria/output, `risk`, five tri-state `consequences` where `null` BLOCKS the plan, `reversible`, `humanReviewRequired`, `assurance`, `quality`, hard literals `tools:"none"`/`sideEffects:"none"`, `dataPolicy`, `latency.endToEndDeadlineMs`; registry schemas; `compileIntelligencePlan` :233-338 — derive consequences → classify work classes → topology `full_dissent` iff assurance required ∨ risk≠standard ∨ any consequence ∨ reversible===false ∨ human review, else `direct` → fail-closed gates clarification_required / policy_blocked / qualification_required → `selectExecutor` (exact work-class set match, quality, timeout ≤ deadline) → `selectAssurance` (≥2 non-maker families, critical path fits deadline) → `sealPlan` id = sha256(canonical)), `src/assurance.ts` (classifier + risk escalation), `src/receipt.ts` + receipt schemas (decision receipt v3 with five protocol versions and four content digests; coordinate receipt v1; verification re-derives every digest from persisted rows), `src/coordination-execution.ts` (work product, execution-run invariants, public projection stripping cost), `src/routing.ts` + the registry FORMAT (not the frozen bootstrap data), `src/pricing.ts` `calculateCostNanos` shape (cached/cache-write/high-context bps) as an extension of `models_pricing.go`.
Re-implement on STRIDE seams: provider adapters (`providers/http.ts` — Anthropic `/v1/messages` adaptive thinking + `output_format` effort/json_schema; OpenAI `/responses` `store:false` + `reasoning.effort` + `text.format`; xAI `response_format` json_schema; structured JSON only, no tools) → STRIDE's LLM client + `provider_breaker.go` (`classifyProviderFailure`, `providerBreakerRegistry`), with coordination OPTING OUT of twin-dial substitution (a twin would violate a plan's pinned `profileId`) or re-sealing the plan; cost → `models_pricing.go` (float USD) with nanos kept inside receipts; reserve→settle + capacity → usage-ledger seats; persistence → the store with `(tenant, idempotencyKey, requestHash)` idempotency and append-only events; `full_dissent` → the goal engine's reviewer/challenger/judge trio with maker-exclusion as a hard constraint; `orchestrator.finalize` verdict rule (all judges pass ∧ no deterministic fail ∧ no unproved evidence ∧ no blocking finding; disagreement surfaced; `buildContinuation` repair actions with `sourceRef`).
Drop: `billing.ts`/Stripe/prepaid credits/webhook/outbox tables; `mcp.ts` + `stride-shadow-client.ts` (in-process calls replace JSON-RPC); Express/Node; the ~120 qualification-campaign phase scripts (port the registry format, regenerate evidence natively); `site/`. Existing surfaces: NO dashboard (201 CLI scripts; `/readyz`); the value-proof exercise (`scripts/value-proof.ts`: 40 calls, `uniqueCatchCriterionIds` blind-spot metric, `actionAttributionRate`, `falsePositiveActionCount`, `repairQualityRate`, `verdictImproved`, `receiptsVerified`; public vs operator report) is the seed for the admin panel's assurance analytics. `docs/STRIDE_SHADOW_INTEGRATION.md` is a written STRIDE↔Dissent conformance spec — the port's acceptance contract.
Decisions the port must make explicit (from the top risks): (1) golden vectors before any receipt code; (2) the shipped registry is `productionEligible:false` — STRIDE seeds a founder-attested registry for Bonfire's own seats (marked `internal`, journaled) and runs native qualification later, so the panel never shows a permanent `qualification_required`; (3) deliverable deadlines are minutes not seconds — critical path at 120 s defaults ≈ 8 min worst case; add a documented `degraded_assurance` mode STRIDE deliberately allows (Dissent does not) with the receipt stamping it; (4) HMAC single secret is fine founder-only; keypair before any external receipt; (5) soften exact-set work-class matching to superset for STRIDE's wordier goals, journaled as a deviation.

### End-to-end verify (critic round 1, 2026-09-02)

Sandbox: binary rebuilt from the working tree (`go build -o $SP/ma.next . && … $SP/ma -addr :3171`, `MEETING_ROOM_PASSWORD=smoke-pass-1234`, no `OPENAI_API_KEY`). Drivers: Playwright (node, headless Chromium with fake media) + curl; assertions on DOM/JSON, screenshots as evidence only. Scripts + evidence: `$SP/e2e/` (`wave3.js`, `wave4.js`, `wave5b/c/d.js`, `wave7.js`, `wave8.js`, `wave9.js`/`wave9b.js`, `*.png`, `w7-ics*.txt`, `w8-inspect1.json`).

| Wave | Result | Steps / evidence |
| --- | --- | --- |
| 3 Work | **PASS** | Page A idles on `/work`; page B clicks the hub's `[data-studio-app-action="new"][data-studio-app-kind="document"]` → `POST /artifacts/document/new` 201 (`os-artifact-artifacts-1788358015722825000`), editor opens on B. A shows `.studio-project-row[data-project-id=…]` **1.3 s** later with no interaction (os_event path, not the 30 s poll). `POST /assistant/files/save {artifactId}` → 200 `fileId`; `/drive` list shows "Untitled document"; `GET /assistant/files` contains it. `wave3-work-list.png`, `wave3-drive.png`. |
| 4 Studios | **PASS** | Reopened the Wave 3 doc (`savedToFiles:true`, v1). Typed → Save (no Drive modal) → v2, autosave chip un-hides "Saved · 11:53 AM". Typed again → autosave `PATCH /artifacts/document` 200 **1532 ms** after the last key, chip `data-state=saved`, status "Saved" (v3). History button → menu rows `v3 · current / v2 / v1`; picked v2 → version stage → "Restore this version" → PATCH with `restoredFrom:2` → v4, status "Restored from v2", stage closed, body = v2 text. `POST /artifacts/export-docx {artifactId, expectedVersion:4}` → **200**, `application/vnd.openxmlformats-officedocument.wordprocessingml.document`, `attachment; filename="Untitled document.docx"`, 3711 B, `PK` header. Print gate: `studioPrint('document')` → true, `body[data-studio-print=document]` held, second call → false (the "Print is still open" toast path). `wave4-autosave.png`, `wave4-version-stage.png`. Observation (not a defect per D1 wording "after the first successful save"): a reopened, already-filed doc does not arm autosave until one in-session Save — typing alone leaves "Unsaved changes" with the chip hidden. |
| 5 Drive | **PASS** | Uploaded two PNGs as AJ with `visibility=private` → rows carry `visibility:"private"`. `GET /assistant/files` as Tim: neither id present (7 rows); Tim fetching the private blob URL → **404** (AJ → 200). `POST /artifacts/share {fileId}` → `link.url=/a/702V9y…`; anonymous `GET /a/<token>` → **200 image/png 73 B**. Previewer: clicking the tile's name link opens `.drive-previewer[open]` "2 / 2"; `→` clamps at 2/2 (no wrap), `←` → "1 / 2" (blue file), `Esc` → `open:false`. Kebab "Preview" also opens it (DOM click; a Playwright pointer click on that menu item was inert once — timing, not asserted). `DELETE /assistant/files {fileId}` → `{"mode":"trashed"}`, `?scope=trash` lists it, main list does not; `POST /assistant/files/restore` → back in the main list. `wave5-previewer.png`. |
| 7 Around-call | **API PASS / in-room recording FAIL** | `POST /rooms` → `room-26ff02a34691c291`; `POST /assistant/meetings/scheduled` → `sched-4a6b6c7d1d57ecfc`; `GET /calendar/meetings.ics?id=` → `METHOD:PUBLISH`, `SEQUENCE:0`, `UID:scheduled-sched-…@thebonfire.xyz`, `ORGANIZER;CN="AJ":mailto:aj@…`, `STATUS:CONFIRMED`. PATCH `startsAt` → `SEQUENCE:1`, new `DTSTART`. DELETE → 200; ICS → `METHOD:CANCEL`, `STATUS:CANCELLED`, `SEQUENCE:2`. `GET /assistant/meetings/recording/settings?roomId=` → `recordingEnabled:false` (default off, `manageable:true`); PATCH → `true`. Browser: `/video`, `selectLobbyRoom`, lobby `#roomEmptyJoin` → `#appShell.is-in-room`; `#roomRecordToggle` un-hides + enables ("Start recording", `aria-pressed=false`, timer hidden) — consent-gated disc state confirmed. Clicking it flips `is-recording`, then the first chunk `POST /assistant/meetings/recording/upload` → **403 `recording consent is missing: recording consent could not be verified`**; recorder stops with honest copy (toast + `#log`), call continues. **DEFECT (Wave 7 D2):** a signed-in member alone in a manager-enabled room cannot record. Trace: `meetingRecordingConsentDecision` → `effectiveConsentLane` → `consentBindingForPrincipal` returns an error (not a denial) although `data/admission-anchors.json` holds AJ's anchor for this room/sitting (`meeting-20260902-155640-807288000`, written at join). Nothing logged server-side. Owner should check the sitting/principal used at upload vs. the anchor, and `binding.Validate()`/`Authorize` error paths. `wave7-in-room-before.png`, `wave7-recording.png`. |
| 8 Memory | **PASS** (coverage chip skipped) | Private thread `POST /assistant/chat-threads {visibility:"private"}`; message needs a UUID `operationId` (`/messages`), Scout answers "couldn't answer safely" (no key). `POST /assistant/remember {threadId, messageId, text}` → `note-fb106fe93051c21d` (`recorded:true`). `GET /assistant/memory/inspect` lists it (kind `note`, status `active`). Inspector (`/memory` → `setActiveTool('memory')`): row `.memory-item[data-id]` → drawer → Correct → textarea → "Save correction" → `POST …/inspect/action {action:"correct"}` 200 `status:"corrected"`; drawer status "corrected · the previous statement stays auditable". Re-inspect: new ledger row `ldg-correction-note-…` (`entity:correction`, `summary:"correction · closed"`, provenance `anchor:note-…`) **and** the original note still listed with `status:"corrected"` and its thread/message/meeting provenance. Observation: the note's title/summary in the inspector are rewritten to the correction text; the prior wording is not visible on either row (it lives only in the ledger event history). Coverage chip: **skipped** — needs a Scout answer, no provider key in the sandbox. `wave8-drawer.png`, `wave8-corrected.png`. |
| 9 Resilience | **PASS** | No key: `/readyz` typed lanes `degraded`, `scoutText.reason=provider_not_configured`. `#statusPill` → text **"Scout unavailable"**, `data-state=offline`, title "Scout is unavailable — the provider key is not configured on this server"; on a private Scout thread the composer placeholder reads **"Scout is unavailable right now"** (on a channel it stays "message #Bonfire Chat" by design). No "offline" wording anywhere. No env/test hook exists to force `fallback_active`/`paused_by_breaker` (only `OPENAI_API_KEY` is read in `capability_health.go`); those states are covered by Go unit tests `TestProviderBreakerOpenSteersSeatToFallbackAndProbesAfterCooldown`, `TestScoutRouterSameCallFallbackStampsProvenanceAndLaneState`, `TestCapabilityStatusDistinguishesIdleDegradedAndFallbackStates`, `TestAmbientWorkerPausesBehindOpenSeatBreakerAndResumesAsProbe`, and the pill copy ("Scout on a backup model" / "Scout paused") is pinned by `TestIndexScoutStatusPillStatesCopy` in `frontend_scout_status_wave9_test.go`. `wave9-pill.png`, `wave9-composer.png`. |

**Full suite** (`go test -count=1 -timeout 40m .` → `$SP/full-batch-2.txt`, completed): `FAIL github.com/openai/openai-realtime-meeting-assistant 894.965s`, `exit 1`. `--- FAIL` lines (3):
- `--- FAIL: TestDocumentStudioRenderedNestedListsAndSafeImagesRoundTrip (0.74s)` — `frontend_document_studio_test.go:488: rendered Document Studio nested-list/image harness: exit status 1` (`AssertionError: actual 'rgba(255, 255, 255, 0.1)' expected 'rgba(0, 0, 0, 0.1)'` — a theme-dependent colour pin).
- `--- FAIL: TestRoomsFixesRenderedHostMenuDisposalAndMoreLock (0.83s)` — `frontend_rooms_fixes_test.go:310: rendered rooms-fixes harness: exit status 1`.
- `--- FAIL: TestReadinessScoutLanesReportIdleNotDegradedWithHonestShape (0.33s)` — `provider_breaker_test.go:485: workflows status=healthy, want idle`.

## Feed snappiness (hotfix)

Hotfix gen 249 (frontend performance dev, 2026-09-02). AJ: "clicking on conversations and posting stuff, it all still feels laggy"; "when sending a GIF in a chat, they are all stacked and you can't see them until you click"; "users should be able to send videos and photos". Measured on the sandbox with Playwright `performance.now()` marks (`$SP/perf-switch.mjs`: click a thread row → first message paint / settled = `data-projection-state="complete"` + 120 ms DOM quiet) against a seeded 100-message public channel (10 messages carry links, 4 carry GIFs; 80-message tail page). Chromium headless, 1440×900, 10–12 rounds per mode.

**Root causes (thread switch).** (1) Every index poll that carried a newer `updatedAt` for a thread the tab had already loaded DROPPED its messages (`mergeScoutChatIndexRows` → `messagesLoaded:false, messages:undefined`), so the next click on any busy channel showed the "Loading conversation" skeleton until the whole tail round-trip returned — on prod that round-trip is the 4.2 s. (2) `mountDesktopChatLinkPreview` fetched `/assistant/link-preview` for every linked message in the tail at mount (7 server-side page fetches per switch here). (3) The progressive head projection yielded a full animation frame (`rAF → setTimeout`) between every ≤12 ms chunk while assembling into a hidden stage. Read-marker POST (400 ms debounce, macrotask) and typing sync were already off the critical path; `setActiveTool` does no synchronous work of note.

**Fixes.** Index rows keep already-loaded messages as a STALE projection (`messagesStale:true`) → the click renders from memory instantly; `hydrateScoutChatThread` refetches stale rows, merges the tail via `mergeScoutChatMutationPage` (earlier pages survive) and reconciles by key (`scoutChatThreadProjectionKey`: thread id + every message id:render-hash + history seam) — an identical projection never rebuilds the feed. Rows prefetch their tail on `pointerenter`/`focus`; `scheduleScoutChatIdlePrefetch` warms the first four channel rows in `requestIdleCallback` (skipped under `saveData`). Link-preview metadata fetches wait until the card is within 800 px of the viewport (`deferDesktopChatCardFetch`, IntersectionObserver — fetches only, `<img>` stays eager). Head chunks continue on a macrotask.

| Mode (click → settled, 80 nodes) | Before p50 / p95 | After p50 / p95 |
| --- | --- | --- |
| Cold (fresh load, first open; hover + idle prefetch live) | 276 / 339 ms | 44 / 51 ms |
| True cold (all `view=tail` fetches refused until the click) | — | 57 / 96 ms |
| Warm (thread in memory, A→B→A) | 106 / 303 ms | 40 / 132 ms |
| First message paint (cold) | 32 ms | 22 ms |
| Tab switch first paint (chat / drive / rooms / home) | — | 9 / 11 / 9 / 7 ms (max 18) |

Requests during a cold switch: before = tail + 7 × `link-preview` + 1 image; after = tail + blob images only (previews fetch as they scroll into view).

**GIF cards.** Root cause: the `.scout-chat-file--image` frame had no reserved size — until the (1.5 MB+) blob arrived the button was an 8 px sliver (desktop `min-height:0`), so a run of GIFs stacked as blank slivers; tapping opened the lightbox, which loaded the bytes, after which the card "dropped in". Server now stamps `width`/`height` on `/assistant/attachments` and `/assistant/giphy/import` responses (`attachmentImageDimensions`, GIF/PNG/JPEG/WebP header decode) and `sanitizeScoutChatFiles` bounds them on the record; the frame reserves `aspect-ratio: var(--media-aspect)` with a percentage-free width (a shrink-to-fit bubble resolves `%` against an unloaded `<img>` as 0 — that was the collapse) and a shimmer `is-loading` state; unsized legacy frames hold 160×120. GIFs animate on arrival; only under `prefers-reduced-motion` do they hold behind "GIF · tap to play". Pins: `TestIndexChatMediaAttachmentPins`, `TestIndexChatMediaAttachmentsRendered` (bytes held in flight → frames sized before load, no top shift after decode, RM hold + release).

**Photos + videos.** `attachmentUploadSafeMimes` += `video/mp4|quicktime|webm` (container check: ISO BMFF `ftyp` / EBML header; octet-stream declarations sniffed), both model allowlists unchanged (a video never reaches a model — its name/size/duration line does: `Attached file: clip.mov (quicktime, 9303 bytes, video 0:02).`), `blobInlineSafeMimes` += mp4/quicktime, `/artifacts/blob` serves `video/*` through `http.ServeContent` (206 byte ranges — Safari refuses `<video>` without them). Client: `scoutChatVideoUploadable`, 25 MB cap, local `<video preload=metadata>` probe for duration/frame size, inline `<video controls preload="metadata" playsinline>` card at the stored aspect (16:9 fallback) with a compact caption. Verified on the sandbox with mp4 / mov / webm clips (stored `width/height/duration`, card 420×318, `readyState 4`). Drive "Attach from Drive" (`assistantAttachmentFromFileHandler`) admits videos through the same allowlist. Go: `attachments_video_test.go` (allowlist, sniff, dims on upload, model lanes skip video + duration line, blob 206/304).

Trap: the shared tree did not compile during this hotfix (other devs' in-flight `meeting_digest.go`/`blobs.go` edits), so the pattern run + pins were executed in the `hotfix-249` worktree (its own sandbox on :3179, data copied from :3171). Port scripts: `$SP/port-go.py`, `$SP/port-web.py` (anchored, idempotent).
